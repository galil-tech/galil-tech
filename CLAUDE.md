# גדלים למחר 🌱
## מגמת אגרו-טק לגליל המערבי | קובץ זיכרון לקלוד קוד

> קובץ זה נטען אוטומטית בכל שיחה. הוא ה"חוקה" של הפרויקט.

---

## ⚠️ תהליך עבודה פעיל: טיפול במסמך הארות המורה
קיימת תוכנית עבודה מסודרת ומפורטת בקובץ [`.claude/MASTER-WORKFLOW.md`](.claude/MASTER-WORKFLOW.md) לטיפול ב-`docs/teachers-remarks.docx` – תיקון 17 מערכי המורה, עדכון תשתית, בניית לומדות, מצגות, וממשק מלא, עם נקודות אישור אנושיות מפורשות בכל שלב.
**בתחילת כל שיחה הקשורה לטיפול בהארות המורה – יש לקרוא את הקובץ הזה במלואו**, ולקרוא את "יומן התקדמות" בתחתית מסמך זה כדי לדעת באיזה שלב בדיוק עצרנו ומה אושר, לפני שמתחילים לעבוד.

---

## מה הפרויקט
תוכנית אגרו-טק חינוכית, שנבנתה במקור לכיתות ח', עם **התאמה מערכתית גם לבית ספר יסודי (כיתות ה'-ו')**, בגליל המערבי.
17 שיעורים, 4 פאזות, מובילים לתערוכת אבטיפוסים בפברואר.
**משך התוכנית:** תוכנית הבסיס היא ל**חצי שנה**; ניתן להאריך ל**שנה מלאה** ב**תיאום מראש**.
נבנתה **על ידי גליל-טק** (לא "עבור" גליל-טק), תחת **מונא – חלל לשינוי**.
שנת לימודים: תשפ"ז–תשפ"ח.

**כוכב הצפון:** תערוכת האבטיפוסים. מהשיעור הראשון התלמיד יודע לאן הוא הולך.
כל שיעור 2 ואילך מסתיים בשאלה: "איך זה רלוונטי לפרויקט שלי?"

---

## היררכיית מקורות – מה קובע כשיש סתירה
1. **`docs/syllabus.xlsx`** – הסילבוס הרשמי. המקור הקובע לכל פרט קונקרטי: סטארטאפים, לוח זמנים, שאלות מנחות, גיימיפיקציה. אם יש סתירה בין מסמך אחר לסילבוס – **הסילבוס מנצח**.
2. **`docs/vision-2.0.docx`** – מסמך החזון. תמונה כללית, עקרונות, ולוח זמנים שנתי. פחות מפורט מהסילבוס בנושאי שיעור ספציפי.
3. **CLAUDE.md (כאן)** – תמצית מעובדת של שני המקורות + החלטות תהליך עבודה. מתעדכן בכל פעם שמתגלה התאמה/סתירה.

אם מתגלה סתירה חדשה בין המקורות – לעצור, להציג למשתמש, ולא להכריע באופן עצמאי.

---

## מבנה 4 הפאזות
> שמות הפאזות עודכנו לפי הסילבוס הרשמי (`docs/syllabus.xlsx`) – זה המקור הקובע, לא מסמך החזון.

| פאזה | שיעורים | נושא | מוצר ביניים |
|------|---------|------|--------------|
| 1 – נביטה | 1–4 | יסודות: צמח, חקלאי, הידרופוניקה | שתיל אישי + דרכון חקלאי |
| 2 – צמיחה | 5–8 | חיישנים, Arduino, IoT | קוד פונקציונלי + לוח נתונים |
| 3 – פריחה | 9–12 | ביג דאטה, AI, רובוטיקה, GIS | גרף נתונים + פנקס רעיונות |
| 4 – פירות | 13–17 + תערוכה | עיצוב, אבטיפוס, פיץ' | אבטיפוס + פיץ' 3 דקות |

---

## מבנה קבצים בפרויקט

```
galil-tech/
├── CLAUDE.md                  ← אתה כאן
├── docs/
│   ├── vision-2.0.docx          ← מסמך החזון (כללי, פחות מפורט)
│   ├── syllabus.xlsx            ← ⭐ הסילבוס הרשמי - המקור הקובע לכל פרט! (4 גיליונות)
│   ├── data-schema.md            ← תכנון שכבת נתונים ל-Google Sheets
│   └── claude-code-guide.docx   ← מדריך העבודה (גרסת Word)
├── index.html                  ← דף בית מאוחד יחיד (מצב תלמיד/מורה) – ראה הערה למטה
├── lessons/
│   ├── lesson-05.html
│   ├── lesson-06.html
│   ├── lesson-07.html
│   └── lesson-08.html …        ← בפיתוח, אחד לכל שיעור
├── teachers/
│   ├── teacher-intro.html       ← הקדמה כללית למורה (אופי התוכנית, מטרות, מבנה שנה/שיעור, גיימיפיקציה, גמישות+הארות) – לקרוא לפני שיעור 1
│   ├── teacher-01.html … teacher-17.html
│   └── (הסיור – מערך מורה נפרד, טרם נבנה)
├── simulators/
│   ├── simulator.html          ← סימולטור צמח (5 חיישנים)
│   └── arduino-simulator.html  ← סימולטור ארדואינו
├── wall.html                   ← קיר הכיתה + לייקים
├── leaves.html                 ← עלים (leaves_earned, רמות) + לידרבורד בין-קבוצתי כיתתי
├── shop.html                   ← חנות עלים (leaves_balance) – תשתית מוכנה, קטלוג ריק ממתין למילוי פריטים
├── journey.html                ← מסע התלמיד
└── data/
    └── sheets-config.js        ← Google Sheets connection (בפיתוח)
```

### כלל הנתיבים היחסיים (קריטי – לבדוק בכל קישור!)
הקבצים מפוזרים בתיקיות נפרדות. כל קישור `href`/`src` חייב להתאים למיקום הקובץ:

| הקובץ שבו אנחנו | קישור ל-index.html / wall.html / leaves.html / shop.html / journey.html | קישור לקובץ באותה תיקייה | קישור לתיקייה אחרת |
|---|---|---|---|
| `index.html` (בשורש) | `wall.html` (אותה תיקייה) | — | `lessons/lesson-06.html` |
| בתוך `lessons/` | `../wall.html`, `../leaves.html`, `../shop.html`, `../journey.html`, `../index.html` | `lesson-07.html` | `../teachers/teacher-06.html` |
| בתוך `teachers/` | `../wall.html`, `../leaves.html`, `../shop.html`, `../journey.html`, `../index.html` | `teacher-07.html` | `../lessons/lesson-06.html` |
| בתוך `simulators/` | `../wall.html`, `../leaves.html`, `../shop.html`, `../journey.html`, `../index.html` | `arduino-simulator.html` | `../lessons/lesson-07.html` |

**כלל אצבע:** מתיקיית משנה חוזרים לשורש עם `../`, ולתיקיית משנה אחרת עם `../תיקייה/קובץ`.
לפני שמוסיפים קישור חדש בכל קובץ – לבדוק תחילה באיזו תיקייה הקובץ הנוכחי יושב.

### החלטה: index.html יחיד, בלי דפי אינדקס נפרדים
**אין** `lessons.html` ו**אין** `teachers.html` כקבצים נפרדים. `index.html` הוא הדף היחיד שמרכז ניווט:
- במצב תלמיד מציג רשימת שיעורים + סימולטורים + קיר + עלים
- במצב מורה מציג טבלת מערכי שיעור + כלים + סימולטורים

כשבונים שיעור חדש (lesson-NN.html) – יש לעדכן את הרשימה/טבלה הזו בתוך `index.html` ישירות, לא ביצור קובץ אינדקס נפרד.

### החלטה: קבצי תשתית חוצי-שיעורים נשארים בשורש
`wall.html`, `leaves.html`, `shop.html`, `journey.html` הם **לא** ספציפיים לשיעור בודד – הם תכונה משותפת לכל הכיתה/כל השיעורים (קיר אחד, לידרבורד אחד, חנות אחת, מסע תלמיד אחד). לכן הם יושבים בשורש לצד `index.html`, **לא** בתוך `lessons/`. קישור אליהם מתוך תיקיית משנה הוא `../wall.html` וכו'.

---

## ארכיטקטורה טכנית
- **Frontend only** – HTML + Tailwind CDN + Vanilla JS. אין build step.
- **RTL חובה** – `dir="rtl"` על כל `<html>`.
- **אחסון:** `localStorage` (עכשיו) → Google Sheets (קרוב) → Google Classroom SSO (תשפ"ח).
- **Hosting:** GitHub Pages – סטטי, חינם, כתובת קבועה.
- **שמות קבצים:** אנגלית בלבד, בלי רווחים (`lesson-08.html` לא `שיעור 8.html`).

---

## ניהול סרטוני וידאו – שני מסלולים

### מסלול א' – סרטונים שצולמו/הופקו על ידנו (mp4 מקומי)
לדוגמה: `aqua.mp4` (שיעור 6), `arduino.mp4` (שיעור 7).
- **מיקום:** באותה תיקייה כמו קובץ השיעור (`lessons/aqua.mp4` לצד `lessons/lesson-06.html`)
- **שם קובץ:** אנגלית, בלי רווחים
- **הטמעה ב-HTML:**
```html
<video controls class="w-full rounded-2xl" style="max-height:340px">
  <source src="aqua.mp4" type="video/mp4">
</video>
```
- אלה קבצים שלנו או ברישיון מתאים – אין מגבלת זכויות יוצרים.

### מסלול ב' – סרטוני YouTube (embed, לא הורדה!)
**חשוב:** Claude (גם ב-claude.ai וגם ב-Claude Code) לעולם לא מוריד סרטוני יוטיוב – זו הפרת זכויות יוצרים ותנאי שימוש. הדרך החוקית היחידה היא הטמעת iframe.
- יש subagent מוכן: `video-finder` – מחפש מועמדים, **לא מטמיע בלי אישור מפורש**
- פקודה: `/find-video [תיאור הנושא והשיעור]`
- אחרי אישור, קוד ההטמעה:
```html
<div class="yt-wrap">
  <iframe src="https://www.youtube-nocookie.com/embed/VIDEO_ID?rel=0&modestbranding=1"
    title="כותרת בעברית" allowfullscreen></iframe>
</div>
```
שימוש ב-`youtube-nocookie.com` (לא `youtube.com`) – מצב פרטיות מוגברת, מתאים לסביבה עם קטינים.


## עיקרון העבודה בכיתה (חשוב לכל SPA שנבנה!)
> **מחשב = תמיד בזוגות. שתיל = תמיד אישי. חיישנים = לקבוצה (3–4 תלמידים).**

ציוד טיפוסי לכיתה של 30: 8 Arduino, 8 חיישני pH, 10 חיישני לחות/טמפ', 30 שתילים אישיים, 15 מחשבים/טאבלטים.
כל הנחיה ב-SPA צריכה לדבר בלשון רבים לקבוצה ("הקבוצה שלכם"), לא ביחיד.

---

## מערכת הגיימיפיקציה
- **מטבע:** עלים 🌿
- ✅ **"אינפלציית עלים" נפתרה בפועל (שלב 5.5, בוצע לפי `docs/gadlim_claude_code_stage5_and_5_5.md`):** סכימה מאומתת של "סה"כ מקסימום" מכל 17 מערכי המורה = **1383 עלים**. **5 הרמות החדשות (קבועות עכשיו, מבוססות % מהסכום האמיתי):**
  - 🌱 זרע: 0–149
  - 🌿 שתיל: 150–399
  - 🌳 צמח צעיר: 400–699
  - 🌻 פורח: 700–1049
  - 🏆 חקלאי העתיד: 1050+ (≈76% מהסכום – טיפוס נשאר חי עד שיעור 13–15 בערך, לא נגמר מוקדם)
  - מקור: גרסת `index.html` ו-`leaves.html` הישנות חישבו רמה לפי `Math.max(ls5,ls6,ls7)` בלבד (שגוי – זה היה בעיה כשרק 3 שיעורים נבנו). תוקן ל**סכום** `ls1`...`ls17`.
- **שני מונים (P2 בהוראות 5.5):** `leaves_earned` = סכום כל מפתחות `ls{N}` (מצטבר, קובע רמה, לעולם לא יורד). `leaves_balance` = `leaves_earned - leaves_spent` (יתרה פנויה לחנות, יורדת בקנייה בלי להוריד רמה).
- **באדג'ים (9 סה"כ, עודכן בשלב 5.5 לפי G7 במסמך ההוראות):**
  - 🔍 **חוקר שטח** – השלמת סיור + גיליון תיעוד מלא
  - 🌱 **השתלן** – שתל את השתיל האישי (שיעור 3)
  - 💧 **ידיים על המדים** – ביצע מדידת pH+EC תקינה (שיעור 5)
  - 💻 **הקוד עובד!** – קוד ארדואינו פונקציונלי (שיעור 7)
  - 🔁 **סוגר מעגל** – הוכיח מעגל בקרה סגור (שיעור 8)
  - 📊 **מדען נתונים** – הפקת תובנה מגרף נתוני הצמח (שיעור 11)
  - 🛠️ **בנאי האבטיפוס** – השלים אבטיפוס עובד (שיעור 14-15)
  - 🚀 **יזם העתיד** – אבטיפוס + פיץ' בתערוכה
  - 🏆 **חקלאי העתיד** – סיום הקורס + הגעה לרמה 5

  זהו מנגנון מעורבות מרכזי לבני 14 (תחושת אספנות) - מומלץ להעדיף אותו מוקדם יותר ברשימת העדיפויות, לא לדחוף לסוף.
- **חנות עלים (`shop.html`, P3 בהוראות 5.5)** – תשתית בנויה (כותרת, יתרה, רשת כרטיסים, לוגיקת קנייה), **קטלוג ריק בכוונה** – ממתין שהמשתמש ימלא פריטים (`CATALOG` בתוך `<script>`).
- **Side Quests** – טרם נבנו, נמצאים בתכנון
- **לוח מנהיגות (`leaves.html`, P4 בהוראות 5.5)** – נבנה מחדש: **בין קבוצות הפרויקט** (לא בין תלמידים בודדים), כיתתי בלבד עד שיעור 10. מפתח `localStorage`: `groupLeaderboard` (מערך `{name, leaves}`). שיתוף בין כיתות/בתי ספר – וו לעתיד, לא ממומש עדיין (דורש Google Sheets, שלב 9).
- **localStorage keys:** `ls{N}` לעלים פר-שיעור (1–17, מסוכמים ל-`leaves_earned`), `leaves_spent` (לחנות), `groupLeaderboard` (לידרבורד קבוצתי). מפתח `alv{N}` שתועד כאן בעבר **לא היה בפועל בקוד** – הוסר מהתיעוד.

---

## מבנה שיעור קבוע (SPA חדש) – 90 דקות, לפי הסילבוס הרשמי
> תוקן מ-55 דק' ל-90 דק' לפי `docs/syllabus.xlsx` (גיליון "מבנה שיעור") – זה המקור המדויק והרשמי.

1. **פתיחה (15 דק')** – הצגת שאלה מעוררת עניין + חיבור לשיעור הקודם. כלים: מצגת קצרה.
2. **דיון (15 דק')** – שיחה מונחית, דיון מבוסס בעיה, שאלות לחשיבה גבוהה. כלים: דיון בקבוצות קטנות או בפורום מלא.
3. **לומדה (25 דק')** – עבודה בזוגות. הסבר תיאורטי מובנה + למידה דרך שאלות הבנה וחשיבה. כלים: מחשב לכל זוג, דף עבודה מעוצב.
4. **מעשי (30 דק')** – עבודה קבוצתית על הפרויקט הכיתתי: פעילות / ניסוי / בנייה. כלים: מערכת הפרויקט הכיתתי (בקרים, חיישנים, ברזים) + דף עבודה.
5. **נתונים + Teaser (5 דק')** – תיעוד מדידות ביומן הצמח, עדכון לוח הנתונים, הכנסת נתונים ללומדה. כלים: גיליון Google / גרף, מוכנס דרך הלומדה.

**הערה:** "עבודה בזוגות במחשב" (שלב 3) ו"עבודה קבוצתית בפרויקט" (שלב 4) הם **שני הרכבים שונים** – ראה "עיקרון העבודה בכיתה" למעלה. אל תבלבל בין השניים בהנחיות שמופיעות בקוד.

---

## פערים שזוהו בשיעורים 5–7 הקיימים (לתקן ב-Claude Code, לא רטרואקטיבית כאן)
- שיעורים 5–7 נבנו עם לוח זמנים של 55 דק' (טרם תוקן ל-90 דק' המדויקות מהסילבוס)
- ~~שיעור 4 בסילבוס מופיע כשורה "סיור" לפניו (לא ממוספר) – צריך להבהיר אם הוא נספר כמפגש נפרד~~ **הוכרע (2026-06-18):** הסיור הוא מפגש נפרד ועצמאי – יקבל תיקייה משלו ומערך מורה משלו (שם/מיקום מדויק יוחלט בעת הבנייה בפועל). **המשמעות ל-MASTER-WORKFLOW.md:** "17 מערכי המורה" כוללים בפועל עוד יחידה אחת (הסיור) – לזכור זאת בשלבים 1–5 ובהמשך 6–8.
- שיעור 8 (בעת הבנייה) חייב לכלול **גם סגירת מעגל בקרה טכני**: חיישן → בקר → ברז/הודעה (לא רק "סינרגיה" רעיונית כמו שתועד קודם) – זו הערה מפורשת מהסילבוס הרשמי

---

## מבנה מערך מורה (קבוע לכל שיעור)
~~סיסמה אחידה: `agrotech2026`~~ **בוטלה (הוכרע 2026-06-18):** אין יותר סיסמה בכניסה לכל מערך מורה בנפרד – תוסר בפועל מכל הקבצים בשלב 4 (כדי שניתן יהיה לשלוח את המערכים להתייעצות חיצונית בלי מחסום). מנגנון כניסה/הזדהות מלא (אימות חד-פעמי למורה, וגם לתלמידים) ייבנה בשלב 9ג של `.claude/MASTER-WORKFLOW.md`. מערכי המורה הקיימים (05–07) עדיין כוללים את הסיסמה הישנה ויעודכנו גם הם בשלב 4.
סעיפים: מטרות (ידע/מיומנות/חשיבה/גיימיפיקציה) → הכנה וציוד → דיון מודרך → לוח זמנים דקה-דקה → בעיות נפוצות ופתרונות → רקע על הסטארטאפ → גיימיפיקציה (טבלת עלים) → התאמות (תקועים/מתקדמים/ריבוי שפות) → שדה הערות.

---

## סטארטאפים ישראלים לפי שיעור (פעילים, מאומתים)
> מקור: `docs/syllabus.xlsx` (גיליון "סילבוס מלא") + אימות web_search.

| שיעור | סטארטאפ | נושא |
|-------|----------|-------|
| 1 | Vertical Field | חקלאות אנכית |
| 2 | Netafim | טפטוף/השקיה |
| 3 | Supplant | חישה לצמח, מים ודישון |
| 4 | FieldIn | ניהול חקלאי, ניטור שדה |
| 5 | Growee | ניטור pH/EC אוטומטי |
| 6 | Phytech | דנדרומטר – לחץ תא עץ |
| 7 | **Beewise** | כוורת רובוטית (לא Eddy – ראה הערה למטה) |
| 8 | BioBee + חממה סולארית (יבגני) | הדברה ביולוגית + סינרגיה אנרגטית |
| 9 | Taranis | לוויינים, רחפנים, GIS |
| 10 | Tevel Tech | רובוטיקה בקטיף |
| 11 | Prospera → Valmont (מותג Valley) | ביג דאטה חקלאי |
| 12 | (מודל AI חקלאי כללי) | AI בחקלאות |
| 13 | Beewise (הדגמה שנייה) | סיעור מוחות / Design Thinking |

**כלל:** לפני הוספת סטארטאפ – ודא שהוא פעיל (web_search). 

**רשימה שחורה (לא להציג כפעילים):**
- **InFarm** – סגרו פעילות.
- **Eddy / Flux (eddy.farm)** – הסילבוס המקורי ייחס אותו לשיעור 7, אך אומת ב-2026 שכל המקורות הזמינים (NoCamels, Times of Israel, Bangor Daily News, Daily Herald) מתוארכים ל-2016–2017 בלבד, ללא עדות לפעילות מ-2018 ואילך. הוחלט להישאר עם **Beewise** בשיעור 7 (כבר בנוי ומאומת). אם בעתיד עולה שאלה על Eddy – זה כבר נבדק, אין צורך לבדוק שוב אלא אם מתקבל מידע סותר.

**הערת תיקון (2026-06-17):** נמצאה סתירה בין הטבלה הזו ל-`docs/syllabus.xlsx` בנוגע לשיעורים 10 ו-13 – הטבלה כללה בעבר "Tevel Tech + Beewise (הדגמה שנייה)" בשיעור 10 ו"Beewise (הדגמה שלישית)" בשיעור 13, אך הסילבוס הרשמי (העמודה "סטארטאפ ישראלי" בגיליון "סילבוס מלא") מציין במפורש ששיעור 10 הוא Tevel Tech בלבד (Beewise לא מוזכר בו), ושיעור 13 הוא "Beewise (הדגמה שנייה)" – כלומר ההופעה השנייה בפועל (הראשונה בשיעור 7). הטבלה תוקנה בהתאם לסילבוס (המקור הקובע). אם בעתיד עולה שאלה על זה – זה כבר נבדק ואומת ישירות מול ה-XML הגולמי של הסילבוס.

**סטארטאפים מאומתים מחוץ ל-17 השיעורים (הרצאות/בוטקאמפים ייעודיים):**
- **Solargik** – אומת (2026-07-07) עבור הרצאת "אגטק 101" לבוטקאמפ אגרו-וולטאיקה (לא חלק מרצף 17 השיעורים). הוקמה 2021, ירושלים, 100+ עובדים. פיתחה עם SolarEdge (2023) מערכת טרקר סולארי ייעודית לאגרו-וולטאיקה, מאפשרת התקנה על שיפועים עד 30% בלי פילוס קרקע – שימוש כפול בקרקע: אנרגיה+חקלאות. פעילות עדכנית: השקת פלטפורמת AI (SOMA Pro) ב-Intersolar 2025. **הערה:** מספרי גיוס לא אומתו במקור אמין עקבי – אין לציין סכום גיוס ספציפי בתוכן, רק תיאור איכותי.

---

## ספריית פטרנים (patterns) – אל תמציא מחדש!
| Pattern | איפה נבנה לראשונה | שימוש |
|---------|---------------------|-------|
| גרירת בלוקי קוד if/else | lesson-07 שלב 3 | לימוד לוגיקה |
| תרשים מעגלי + גרירה | lesson-05 שלב 2 (מעגל החנקן באקוופוניקה) | תהליכים מעגליים |
| סליידר + תגובה חיה | lesson-05 שלב 3 | חיישנים/ערכים |
| בחירה מ-3 | lesson-07 שלב 4 | גשר בין גרירה לכתיבה |
| השלמת מילה בודדת | lesson-07 שלב 5 | כתיבת קוד ראשונה |
| סימולטור LED | lesson-07 שלב 6 | תוצאות חיות |
| תרחישי אבחון | simulator.html טאב 3 | חשיבה ביקורתית |
| SVG לוח ארדואינו | arduino-simulator.html | חיבורים ויזואליים |

---

## ממשק רב-כיתתי ובין-בית-ספרי (חזון, בבנייה)
- **קוד כניסה לתלמיד:** 4 ספרות, ללא סיסמה (שלב א'). Google Classroom SSO (שלב ב', תשפ"ח).
- **URL params מתוכננים:** `?school=maale&class=8a&student=1234`
- **לוח נתונים משותף:** כל בית ספר מעלה מדידות שבועיות, גלוי לכולם.
- **אתגר חודשי בין-בית-ספרי:** "מי הצמיח הכי גבוה?" בלוח מנהיגות.
- **גלריית אבטיפוסים בתערוכה:** הצבעה בין-בית-ספרית.
- **Backend מתוכנן:** Google Sheets (Apps Script) – חינם, מורים רואים בזמן אמת.

---

## עדיפויות פיתוח נוכחיות (בסדר הזה)
> ✅ **סתירת סדר העבודה הוכרעה (2026-06-18):** המשתמש אישר במפורש שהסדר הקובע הוא זה שבתוכנית `.claude/MASTER-WORKFLOW.md` – קודם **כל** 17 מערכי המורה, ועל בסיסם בלבד נבנות הלומדות (SPA) והמצגות; Google Sheets / GitHub / שיתוף בין-כיתתי מגיעים בסוף (שלב 9). הרשימה למטה עודכנה בהתאם.

1. ~~עדכון שם הפרויקט לכל מקום: "גדלים למחר"~~ **בוצע (2026-06-18)** – הוחלף "גידול העתיד" ב-"גדלים למחר" ב-6 קבצים: `index.html`, `lessons/lesson-05/06/07.html`, `simulators/simulator.html`, `simulators/arduino-simulator.html`. (השם "חקלאי העתיד" נשאר בכוונה – זה שם הבאדג'/התערוכה, מושג נפרד ומאומת, לא שגיאה.)
2. הוספת שאלת "כוכב הצפון" וTeaser ברור לסוף כל שיעור קיים (5, 6, 7) – פער רטרואקטיבי, ראה `sequence-checker`
3. **תוכנית טיפול בהארות המורה (`.claude/MASTER-WORKFLOW.md`) – התהליך הפעיל כרגע, בסדר הזה בדיוק:**
   1. סיום **כל** 17 מערכי המורה (`teachers/teacher-NN.html`) לפי הארות המורה
   2. עדכון קבצי תשתית/מערכת לפי המאושר במערכי המורה (כולל סכמת נתונים אחידה, ראה `docs/data-schema.md`)
   3. בניית **כל** קבצי הלומדה (`lessons/lesson-NN.html`) – רק על בסיס מערכי המורה המאושרים, לא לפני. **אמת כל סטארטאפ לפני בנייה, לא תוך כדי.**
   4. בניית שקפי פתיחה לכל שיעור
   5. GitHub Pages → סכמת נתונים סופית → כניסות/הרשאות → Google Sheets → שיתוף בין-כיתתי/בין-בית-ספרי (תתי-שלבים 9א–9ו, כולל לוח נתונים בין-בית-ספרי ופרופיל תלמיד עם קוד 4 ספרות)
   6. בקרת איכות כוללת (שלב 10)
4. ~~באדג'ים + חנות עלים + Side Quests – תועדף גבוה~~ **חלקית בוצע (שלב 5.5, 2026-06-29):** 9 באדג'ים מוגדרים + `shop.html` נבנה כתשתית (קטלוג ריק). **נותר:** Side Quests (טרם נבנו) ומילוי קטלוג החנות בפועל ע"י המשתמש.
5. גרסה דו-לשונית (עברית + ערבית) – תשפ"ח

---

## עבודה עם Git – חובה מהיום הראשון
Claude Code יכול להריץ פקודות git בשבילך. כללים:
- **לפני כל שינוי משמעותי** (יותר מקובץ אחד, או שינוי מבני) – לבקש ממני `git commit` של המצב הנוכחי, כדי שתהיה נקודת חזרה
- **קונבנציית הודעות commit:** בעברית, תיאור קצר וברור (לדוגמה: "הוספת שיעור 8 - סינרגיה", לא "update")
- **לפני העלאה ל-GitHub Pages** – לבדוק שכל הקבצים שצריך נמצאים ב-git (לא רק נשמרו על הדיסק)
- לבקש ממני "תבדוק את היסטוריית ה-git" אם משהו "התקלקל" ואתה רוצה לראות מה השתנה

**מצב בפועל (מ-2026-08-05):** repository קיים ב-GitHub בכתובת `github.com/galil-tech/galil-tech` (public), branch `main`. GitHub Pages פעיל (build type `legacy`, פריסה ישירה מ-`main`) בכתובת החיה: **https://galil-tech.github.io/galil-tech/**. `lessons/aqua.mp4` (134MB) לא נכנס לריפו – מעל מגבלת 100MB לקובץ בודד ב-GitHub, נמצא ב-`.gitignore` וממתין לפתרון (Git LFS או אחסון חיצוני) לפני שהוא ייכלל.

---

## פאנל המומחים – `/expert-panel`
בדיקת עומק רב-תחומית לקובץ שיעור/מערך מורה או לתשתית הפיתוח עצמה, ע"י שישה subagents קבועים. כל מומחה כותב בגוף ראשון, ומחזיר דיווח קצר בפורמט אחיד: כותרת+אמוג'י, מה עובד טוב, סיכון/בעיה, הצעה קונקרטית. בסוף, מיכל מסנתזת את כל הדוחות לתמונה אחת.

| מומחה | קובץ | תפקיד | כלים |
|-------|------|-------|------|
| 🎓 פרופ' יובל | `prof-yuval-pedagogy.md` | פדגוגיה ורצף למידה – Teaser, כוכב הצפון, עומק קוגניטיבי לגיל, עקביות מושגית | Read, Grep |
| 🔧 ד"ר ענת | `dr-anat-practical.md` | מעשיות, ציוד ובטיחות – חיבורי חשמל/ארדואינו, הרכב עבודה (זוגות/אישי/קבוצה), בטיחות | Read, Grep |
| 🎮 נועה | `noa-gamification.md` | גיימיפיקציה וחוויית משתמש – אינפלציית עלים, משוב מיידי, גרירה/מגע, ספריית פטרנים | Read, Grep |
| 🇮🇱 אורי | `uri-startups.md` | סטארטאפים וסיפור עסקי – דיוק עובדתי, אימות בספק, רשימה שחורה | Read, Grep, WebSearch, WebFetch |
| ⚙️ דנה | `dana-claude-code-expert.md` | מומחית Claude Code – תקינות תצורה (`matcher` כמחרוזת, לא מבנה!), כפילויות, הזדמנויות אוטומציה | Read, Grep, WebSearch, WebFetch |
| 🧩 מיכל | `michal-holistic.md` | סינתזה הוליסטית – דפוסים חוצים, תלויות, סיכוני עומס, מה דורש הכרעה אנושית | Read, Grep |

**הפעלה:**
- קובץ שיעור/מערך מורה → יובל + ענת + נועה + אורי, ואז מיכל מסנתזת.
- תשתית פיתוח (`.claude/`, `settings.json`) → דנה בלבד, ואז מיכל מסנתזת.

שימוש: `/expert-panel lessons/lesson-08.html` או `/expert-panel .claude/settings.json`

---

## "מוכן להצגה" – קריטריון סיום לכל שיעור (Definition of Done)
שיעור נחשב מוכן להצגה בכיתה רק כאשר **כל הסעיפים הבאים** מתקיימים:
- [ ] עבר `/lesson-review` (בדיקת קוד אוטומטית) בלי באגים פתוחים
- [ ] עבר בדיקת `sequence-checker` (רצף פדגוגי לשיעור הקודם/הבא)
- [ ] כל סטארטאפ מוזכר אומת כפעיל (`verify-startup`)
- [ ] **אתה (ראש המגמה) קראת את השיעור בעצמך מקצה לקצה** – ה-QA האוטומטי בודק קוד ורצף, לא טעם פדגוגי או דיוק תוכן מהותי
- [ ] מערך המורה המתאים קיים ומקושר
- [ ] נבדק במצב נייד (responsive) אם הכיתה משתמשת בטאבלטים

---

## כללים שאסור לשבור
- אסור להמציא נתונים על סטארטאפים – לבדוק עם web_search לפני כל הוספה.
- אסור Unicode bullets בקוד HTML/JS – Tailwind + סמנטיקה תקינה.
- כל שיעור חדש **חייב** לכלול: מסקוט, גיימיפיקציה עלים, קישור לשיעור קודם/בא, ושאלת כוכב הצפון.
- בדוק נגישות RTL בכל רכיב חדש (כיווני SVG, טקסט דו-כיווני בקוד).
- לפני שמשנים קובץ קיים בהיקף גדול – הרץ `/lesson-review` (ראה סקילס).

---

## יומן התקדמות – תוכנית טיפול בהארות המורה (לפי `.claude/MASTER-WORKFLOW.md`)
> מתעדכן בסוף כל שיחה העוסקת בתוכנית הזו, וגם בסיום כל שיעור בודד בשלב 6 (כדי שאם שיחה מתארכת ונפתחת שיחה חדשה באמצע, יודעים בדיוק על איזה שיעור עצרנו).

- **שלב נוכחי:** שלב 1 הושלם ואושר (כולל מענה על כל שאלות ההבהרה, 2026-06-18). לעבור לשלב 2.
- **מה אושר עד כה:** שילוב התוכנית ל-CLAUDE.md. סדר העבודה הקובע הוא זה שב-MASTER-WORKFLOW.md. `docs/teachers-remarks.docx` חולץ לקובץ טקסט עזר ב-`.claude/teachers-remarks-extracted.txt` (הקובץ המקורי docx, ל-Read אין גישה ישירה לבינארי – אם נפתחת שיחה חדשה ואין את קובץ הטקסט, יש לחלץ מחדש מה-docx: Add-Type System.IO.Compression.FileSystem → ExtractToDirectory → word/document.xml → strip תגי XML). שלב 1: 73 הערות (11 כלליות + 62 לפי שיעור) סווגו א/ב/ג עם דגלי גלובלי/שרשרת.
- **תשובות שאלות הבהרה משלב 1 (2026-06-18, כל ההכרעות הבאות תקפות להמשך):**
  1. סיסמת מורה `agrotech2026` – **בוטלה כבר עכשיו** מכל מערכי המורה (כולל 05–07), כדי לאפשר שליחה להתייעצות חיצונית; מנגנון הזדהות מלא נבנה בשלב 9ג. (עודכן ב"מבנה מערך מורה" למעלה.)
  2. שתילים לשיעור 3 (לבחור בשיעור 2 או בסיור) – נשאר כניסוח "או-או" אינפורמטיבי למורה בטקסט, לא דורש הכרעת קוד.
  3. "מעגל משיעור 5" (לשיעור 8) – אומת בפועל: זה מעגל החנקן באקוופוניקה ב-`lessons/lesson-05.html` שלב 2 (`בן את מעגל החנקן!`). תוקן בטבלת ספריית הפטרנים למעלה (היה רשום שגוי כ-lesson-06).
  4. "קיר סיעור מוחות" (שיעור 9) – יתווסף כפיצ'ר **חדש** בתוך `wall.html` הקיים (לא קובץ נפרד), לצד פיצ'ר הלייקים.
  5. "קתימב\"ה" (שיעור 16) – אקרונים עברי לטכניקות דיבור מול קהל: **נ**יסוח, **ס**ביבה, **ק**ול, **ת**נועה, **י**דיים, **מ**בט, **ב**טחון. לשימוש בתוכן השקף בשלב 8.
  6. הסיור – מפגש נפרד, תיקייה ומערך מורה משלו. (עודכן ב"פערים שזוהו" למעלה.)
  7. שם הפרויקט – טעות הקלדה, תמיד "גדלים למחר" (אין שינוי).
- **שלב 2 הושלם (2026-06-18):** פאנל מומחים (יובל+ענת+נועה+אורי, סינתזה ע"י מיכל) רץ על תוכן `docs/teachers-remarks.docx` עצמו. עומדים ב"🛑 עצירה 2", הושלם.
- **תשובות עצירה 2 (2026-06-18):**
  1. **נוסחת רמות גיימיפיקציה** – הוכרע (לא 80% דינמי): לסכום עלים מקסימליים מכל 17 השיעורים ולהתאים סקאלה+באדג'ים למספר הכולל. (עודכן ב"מערכת הגיימיפיקציה" למעלה.) חישוב מספרי בפועל – רק אחרי שלב 4.
  2. **סיסמת מורה** – אין צורך במצב ביניים. מבטלים אותה כרגע כמתוכנן, חוזרים לנושא ההזדהות המלאה בשלב 9ג בלבד (הסיכון של חשיפה זמנית התקבל במודע ע"י המשתמש).
  3. **שיעור 2 – אזור אחריות קבוצתי** – לבחירת המורה; מערך המורה ימליץ על רוטציה בין שיעורים אך לא יכריח.
  4. **שיעור 4 – "המשלה לעלים"** – רעיונית/אנלוגיה בלבד, לא מטבע שני עם שקילות מספרית.
  5. **שיעור 9 – פנקס רעיונות / קיר סיעור מוחות** – דיגיטלית זה מאגר אחד מאוחד: "קיר סיעור מוחות" (פיצ'ר חדש ב-wall.html, כבר הוכרע בעצירה 1). "פנקס הרעיונות" בלומדה הוא רק ה-UI/האינטראקציה בצד הלומדה שכותבת לאותו מאגר, לא מאגר נפרד.
- **נקודות ביצוע פתוחות לשלב 3 (לא דורשות הכרעת משתמש נוספת, רק ליישם בתוכנית המילוי):** שיעור 13 – לתכנן חלוקת זמן מפורשת לכל שלב ב-Design Thinking (Empathize/Define/Ideate+POV). שיעור 14 – לפצל בין משבצות קנבה "מפתח" (משחק מלא) לשאר (דוגמה בלבד), לא לחצות מגבלת 2-3 משחקים/שיעור. שיעור 9 – לנסח כך שלא ישתמע שינוי תיוג הפאזה "פריחה" בקוד. שיעור 9 (Taranis) – לנסח "רחפנים וגם מטוסים קלים מאוישים בעלי כנף קבועה", לא "Tayara/טייארה" כשם מוצר. שיעור 12 (Beewise+AI) – אומת, מאושר להכנסה ללא שינוי.
- **שלב 3 הושלם ואושר (2026-06-18)**, עם שינוי אחד: **קובץ הסיור נדחה למועד מאוחר יותר** (המשתמש עדיין לא יודע את כל הפרטים לגביו) – בכל מקום שמתייחס לסיור בשיעורים 2/3/4, רק להניח/לרמוז שיהיה מערך מורה מפורט לסיור בעתיד, לא לבנות אותו עכשיו ולא לנסות לפרט את תוכנו.
- **שלב 4 הושלם (2026-06-18) – כל 15 היחידות עודכנו** (`teachers/teacher-01.html` עד `teacher-17.html`, חוץ מהסיור שנדחה). תקציר לכל קובץ:
  - **05/06/07** – רק מעבר גלובלי: הסרת סיסמה, ניסוח "בסוף השיעור התלמיד..." בלי "יוכל".
  - **01** – מטרת מהלך-הקורס; שקף חדש "מבנה התוכנית"; מפרט לומדה: טוטוריאל זוגות + משחק גרירה "שרשרת אספקה" (אבוקדו, מקושר לשיעור 10); הסרת "המערכת לא מורכבת"; אופציית צילום.
  - **02** – הרכבה ע"י מתקין מקצועי (לא תלמידים); אזורי אחריות לקבוצות עם המלצת רוטציה; תוכן פרמהקלצ'ר (3 אחיות, זיתי דרוזים, מקושר לשיעור 8); טפטפת-מבפנים בלומדה; הערת רכש שתילים; פלייסהולדר הכנה לסיור.
  - **03** – רזרבת שתילים עודכנה ל-20% (~6); פלייסהולדר התייחסות לסיור; הסבר מאויר לפוטוסינתזה לפני גרירה (מקושר לשיעור 10).
  - **04** – מטרה+פעילות כימות כספי (מים/דשן÷שתילים); אנלוגיית עלים רעיונית בלבד (לא מטבע שני); אחריות מורה על דישון/pH; ציון מעבר נביטה→צמיחה + דיון פרויקט גמר.
  - **08** – ניסוח "סינרגיה" ולא "אקוופוניקה"; לומדה מביאה את מעגל החנקן מ-lesson-05 (לא lesson-06, תוקן) + שכבת "מי מרוויח"; משחק סינרגיה נוסף; שדה רעיונות פתוח.
  - **09** – מיומנות ציור-מאקרו נוספה; Taranis מנוסח כ"רחפנים וגם מטוסים מאוישים"; הערת איחוד "קיר סיעור מוחות"/wall.html; תיוג פאזה 3 נשאר ללא שינוי.
  - **10** – לומדה משלבת שרטוטים משיעורים 1+3+5+8 ("איפה רובוט נכנס"); גרף סיום כבסיס לשיעור 11.
  - **11** – הוסר "הזנה חוזרת" של דרכון; נוסף לימוד סוגי גרפים + נתוני התלמיד עצמו + תוצאות כיתתיות; הערת תשתית להיסטוריית דרכון מלאה (לשלב 9ב).
  - **12** – תוכן פרומפט נכון + טיפים ל-AI; "Beewise+AI" נוסף (מאומת); הכוונה מוכנה מראש למתקשים.
  - **13** – הוספת הקשר וידאו-הוגה+סיור Beewise/בית העמק; הסבר POV למורה; שאלות מנחות נוספות ל-Design Thinking (Empathize/Define/Ideate נשארו עם זמן מוקצה קיים).
  - **14** – נושא Proof of Concept נוסף; קנבה מפוצל ל-2-3 משבצות "מפתח" (משחק מלא) מול שאר (דוגמה בלבד); רשימת חוסרים+תזכורת מורה; אחסון אבטיפוסים.
  - **15** – חריגה מתועדת מהמבנה הסטנדרטי: לומדה 5-10 דק' (לא 25), פרויקט מורחב ל-45 דק'; הדגשת "לסיים היום".
  - **16** – שקף "נ"ס קתימב"ה"; לומדה הורחבה ונהפכה למרכז העבודה (כולל משימת צילום+טיימר מובנה); חזרה גנרלית; תזכורת לא לשכוח פרויקט הכיתה.
  - **17** – "לימודי המשך" הורחב מעבר לחטיבה עליונה; שאלון משוב + דיון עתיד-המערכת נוספו ללומדה; אופציית פרסים.
  - בכל 15 הקבצים: ביצעתי בדיקת grep ידנית לאימות שלא נשארו הפניות שבורות לסיסמה/`unlock()`/`class="hidden"` (לא הופעל `lesson-qa` באופן גורף – הצ'קליסט שלו מיועד ל-SPA אינטראקטיבי עם ניווט/quiz/drag, שלא קיים במערכי מורה סטטיים).
- **שלב 5 + 5.5 בוצעו (2026-06-29), לפי `docs/gadlim_claude_code_stage5_and_5_5.md`:** המשתמש עבר בעצמו על כל 17 מערכי המורה וסיכם את התיקונים בקובץ ייעודי (חלק 0: החלטות נעולות; חלק 1: פקודות גלובליות G1–G7; חלק 2: צ'קליסט פר-קובץ; חלק 3: כלכלת העלים P1–P4). ביצעתי את **כל** ההוראות בקובץ:
  - **18 קבצי המורה** (`teacher-intro.html` + `teacher-01.html`...`teacher-17.html`): דרכון דיגיטלי בלבד (G1); "לומדה" בכל מקום שהופיע "SPA" מול תלמיד, כולל יישור 05/06/07 לתבנית האחידה (G2-G3); כותרות פאזה אחידות (G4); ערכת חומרה רזרבית בשיעורים 5/6/7/8/14 (G5); מכניקת משחק בעלת שם לכל 17 השיעורים (G6); 4 באדג'ים חדשים בנוסף ל-5 הקיימים (G7). פר-קובץ: סעיף ♿ התאמות נוסף ל-05 (היחיד שחסר) + הערת "שיעור עמוס"; "5 מדדים" (לא "5 צרכים") בשיעור 6 עם גשר משיעור 3; הדגמת LED שרוף בשיעור 7 הוחלפה בפלייסהולדר טקסטואלי למורה (להביא תמונה/סרטון בעצמו – אין לי קובץ מדיה ולא הורדתי מהאינטרנט); "רמפת השלמה" נוספה בשיעור 8; חיזוק אזהרת הרשאות AI לבני 14 בשיעור 12 (חוסם פוטנציאלי); "נ"ס קתימב"ה" (7 רכיבים) פושט ל-4 טכניקות מסירה ברורות בשיעור 16 (שיקול דעת שלי, כפי שההוראה ביקשה); עדכון ניסוח "180" ל"1050+" ורשימת 9 התגים בשיעור 17.
  - **כלכלת העלים (5.5):** סכמתי את "סה"כ מקסימום" המדויק מכל 17 הקבצים (אומת ישירות מהטקסט, לא משוערך) = **1383 עלים**. רמות חדשות: 0/150/400/700/1050 (פירוט מלא ב"מערכת הגיימיפיקציה" למעלה). יושמו שני מונים `leaves_earned`/`leaves_balance`. `index.html` ו-`leaves.html` תוקנו לסכום `ls1`...`ls17` (לא `Math.max` כמו קודם). `leaves.html` נבנה מחדש כלוח מנהיגות **בין-קבוצתי** כיתתי (לא אישי) + תצוגת רמה/יתרה; `shop.html` נבנה כקובץ נפרד חדש (תשתית בלבד, קטלוג ריק) – **ממתין שהמשתמש ימלא פריטים בעצמו**. הוסרה החנות החלקית/placeholder שהייתה קודם משובצת בתוך `leaves.html`.
  - לא הופעל `/lesson-review` באופן גורף על קבצי מורה סטטיים (כמו בשלב 4, מאותה סיבה: הצ'קליסט שלו מיועד ל-SPA אינטראקטיבי).
- **שלב 6 – בניית הלומדות (התחיל 2026-07-01):**
  - **lesson-01.html ✅ (2026-07-01):** נבנה ועבר QA ללא בעיות. 6 שלבים: (1) טוטוריאל + הימור האוכלוסייה (bet mechanic), (2) Vertical Field (מאומת) + 2 שאלות, (3) השוואה שדה/אנכי + שאלת הבנה, (4) שרשרת האספקה drag game (7 שלבים, מקושרת לשיעור 10), (5) דרכון חקלאי דיגיטלי (localStorage passport1), (6) סיכום + סיור מערכת + כוכב הצפון + Teaser. מקסימום 50 עלים, localStorage key: ls1. מוצג בindex.html.
  - **lesson-02.html ✅ (2026-07-01):** 6 שלבים: (1) גשר + פרמה-קולצ'ר + 3 אחיות + שאלה, (2) ציר-ידע drag game (4 אבות-דרך בציר זמן + שאלה), (3) נטפים startup + טפטפת מבפנים + 2 שאלות, (4) חישוב 90% חיסכון מים, (5) פרויקט סיור מערכת + כניסת דרכון, (6) סיכום + כוכב הצפון + Teaser. מקסימום 60 עלים, key ls2. Netafim מאומת.
  - **lesson-03.html ✅ (2026-07-01):** 6 שלבים: (1) גשר + 4 צרכי הצמח + שאלה, (2) פוטוסינתזה drag (4 slots למשוואה: מים/CO₂/סוכר/O₂) + שאלה, (3) Supplant startup + 2 שאלות (cascade), (4) זיווג סימפטומים→צורך חסר, (5) שתילת שתיל אישי (checklist + badge "השתלן") + דרכון שתיל, (6) סיכום + כוכב הצפון + Teaser. מקסימום ~65 עלים, key ls3. Supplant מאומת.
  - **lesson-04.html ✅ (2026-07-01):** 6 שלבים: (1) גשר + חקלאות כעסק + שאלה, (2) כרטיס עלויות מדורג (90÷30=3₪/שתיל, 10-3=7₪ רווח) + שאלת הפסד, (3) FieldIn startup + 2 שאלות cascade, (4) תרחיש גילוי מחלה יום שני/שישי + שאלה, (5) מדידת שתיל ראשונה checklist + כניסת דרכון, (6) סיכום + ציון דרך "סיום פאזה 1" + כוכב הצפון + Teaser לפאזה 2. מקסימום ~65 עלים, key ls4. FieldIn מאומת.
  - **lesson-08.html ✅ (2026-07-02):** גשר+סינרגיה+2Qs cascade; מפת סינרגיה (3 כרטיסי-קליק); BioBee+2Qs; קוד if/else (2 שאלות מילוי blanks); סגירת מעגל בקרה checklist+badge; סיכום. ~85 עלים, key ls8.
  - **lesson-09.html ✅ (2026-07-02):** Taranis+2Qs; חוקר מרחוק (detective grid 3 תרגילים); ציור-מאקרו canvas; פנקס רעיונות (3+ רעיונות+passport). ~65 עלים, key ls9.
  - **lesson-10.html ✅ (2026-07-02):** Tevel Tech+2Qs cascade; דיון-קרב yes/no+נימוק; שרטוטים+Q; גרף נתונים ראשון+passport. ~50 עלים, key ls10.
  - **lesson-11.html ✅ (2026-07-04):** ביג דאטה+Q; 3 גרפים+Q; Prospera/Valley+2Qs cascade; גרף-שלי; תובנה+badge "מדען נתונים" (20עלים)+passport. ~55 עלים, key ls11.
  - **lesson-12.html ✅ (2026-07-04):** AI יכולות+Q; תפוס את ההזיה (3 תרגילים); Beewise+AI+2Qs cascade; "שאל את ה-AI" (שאלה+תשובה+השוואה); הנחיה למתקשים+Q+passport. ~55 עלים, key ls12.
  - **lesson-13.html ✅ (2026-07-05):** DT intro+Q; Empathize+Q; Define/POV statement save (15עלים); Ideate "כמה רעיונות בדקה" (טיימר+5 שדות); Beewise inspiration + בחירת רעיון+passport. ~65 עלים, key ls13.
  - **lesson-14.html ✅ (2026-07-05):** PoC vs MVP+Q; Lean Canvas 3 תאים+Q; הכנה+Q; לוג בנייה checklist 3+ (30 עלים); passport. ~75 עלים, key ls14.
  - **lesson-15.html ✅ (2026-07-05):** Fail Fast+Q; מבחן הצלחה save (10); בדיקה+תיקון checklist+fix doc; badge "בנאי האבטיפוס"+passport. ~55 עלים, key ls15.
  - **lesson-16.html ✅ (2026-07-05):** 4 טכניקות+Q; מבנה פיץ'+Q; טיוטת פיץ' (4 שדות); תרגול+טיימר+"קהל בוחר"+משוב (20עלים)+passport. ~55 עלים, key ls16.
  - **lesson-17.html ✅ (2026-07-05):** רפלקציה+Q; מיפוי אישי; סגירת דרכון; שאלון+מכתב; טקס תגים+badge "חקלאי העתיד"; showTotals() מציג סה"כ עלים כל השנה. ~55 עלים, key ls17.
  - בכל הלומדות: init() משחזר flags מ-localStorage (תוקן לאחר QA), גרירה כוללת touch events, כוכב הצפון בשלב 6, קישורי ניווט תקינים.
- **שיעורים 05/06/07:** קיימים מלפנים. בשלב 7 (סשן תיקוני לומדות) המשתמש ישלח תיקונים עליהם + על שאר הלומדות.
- **P1 – תשתית (בוצע 2026-07-05/06):** `assets/core.js` + `assets/core.css` + `assets/points.config.js` נוצרו. lesson-01 עוּבד לחלוטין ל-core.js (reference implementation, QA עבר). שאר 16 הלומדות קיבלו script tags של core.js. lesson-05/06/07 קיבלו גם `GC.pushPassport` לנתוני מדידה (P1.3). nav תוקן ב-wall/shop/leaves. journey.html נבנה מחדש כ-dynamic עם רמות נכונות וגרף לפי שיעור.
- **P2 – גיימיפיקציה (בוצע 2026-07-06):** כל הפיצ'רים ב-core.js ו-`assets/questions.js`:
  - P2.1 PlantTwin (auto-inject, avatar צמח שגדל עם עלים, tooltip, אזהרת חוסר-פעילות)
  - P2.2 Combo multiplier (×1.5 אחרי 3, ×2 אחרי 5 תשובות ברצף)
  - P2.3 Side Quest framework (8 quests מוגדרים, חובה בשיעורים 13+15, auto-register)
  - P2.4 Question Bank (7 שאלות אלטרנטיביות לשיעורים 5/6/7, 2 רנדומליות/שיעור עם seed-based rotation)
  - P2.5 Achievements (6 הישגים) + Streak (בונוס כל 3 שיעורים ברצף)
  - P2.6 Sound toggle (Web Audio API, ברירת מחדל: כבוי, נשמר ב-localStorage)
  - `GC.completeSideQuest('sq-13-pov')` ו-`'sq-15-fix'` מחוברים בלומדות 13/15.
  - `questions.js` נוסף לכל 17 הלומדות.
- **P3 – שיפורים ונגישות (בוצע 2026-07-06):**
  - P3.4: `teacher-dashboard.html` — קרא-בלבד, localStorage, PlantTwin, MSK bars, Achievements, per-lesson bars, growth chart, group LB, passport data, export JSON + import JSON (לצפייה בנתוני תלמיד אחר). קישור נוסף לindex.html.
  - P3.1: lesson-14 autosave Lean Canvas (debounce 800ms); lesson-16 pitch timer ← countdown חי (m:ss) + אדום ב-30 שניות + שמע בסיום; lesson-17 badge ceremony — confetti + achievement sound.
  - P3.2: lesson-17 survey — star rating (1-5 ⭐) + "ממליץ לחבר?" (כן/אולי/לא) + שמירה ל-passport1.
  - P3.3: keyboard navigation — quiz opts מקבלים tabIndex+role="button"+Enter/Space; MutationObserver לאלמנטים דינמיים; aria-label ל-PlantTwin + sound btn. מוטמע ב-core.js כ-_initA11y() המופעלת אוטומטית.
  - P3.1 גל שני (2026-07-06): lesson-07 → שמירת השלמת קוד ל-passport1; lesson-09 → undo button לציור-מאקרו (ImageData history); lesson-10 → "שנה דעה" 30 שניות עם countdown חי; lesson-11 → ניתוח מגמה מתחת לגרף (📈/➡️/📉, ממוצע, cm/שיעור); lesson-05 → pH+EC נלכדים ב-rec ומועברים ל-passport1; lesson-12 → הוספת דוגמת הזיה 4 (EC מודד חמצן = שגוי), completion check דינמי.
- **השלב הבא (הוכרע 2026-07-29):** upgrade_v2 Phase 1+2+3 הושלמו. **בהחלטת המשתמש, לפני שלב 9, נפתח "שלב מצגות"** – בניית שקף-מצגת (`presentations/lesson-NN-slides.html`) לכל אחד מ-17 השיעורים, על בסיס `presentations/master-strategy-mtzagot.md` (brief מפורט שהמשתמש הכין מראש, כולל ספריית 7 תבניות שקף A-G ואנימציה מאושרת ל-2 קבצי דמו: `phase0-template-review-v2.html`, `animation-demo.html`).
  - **מנוע משותף:** `presentations/assets/slides-core.css` + `slides-core.js` — כל 17 קבצי המצגת חולקים אותו מנוע ניווט/אנימציה (ניווט בחצים/רווח, בנייה מדורגת לפי `data-step`, מונה עולה+עמודות גדלות לתבנית A, VS פועם לתבנית D, קו-נמתח SVG, מסך מלא). זה מאפשר תיקון מבני אחד שמשפיע על כל 17 הקבצים בסבב התיקונים הבא, במקום 17 תיקונים נפרדים.
  - **17 קבצי המצגת נבנו (2026-07-29)**, תוכן נשאב ישירות מ-`teacher-NN.html` (ולעיתים גם `lesson-NN.html`) לכל שיעור, לא הומצא. מיפוי מדויק כמו בסעיף 7 של הברייף (מס' שקפים ותבנית לכל שיעור), כולל 3 שקפי "ציון דרך" (9, 11, 13) ושקף גלריה ריק לשיעור 17.
  - **`index.html`** עודכן: טור "מצגת" חדש נוסף לטבלת מערכי השיעור במצב מורה, עם קישור `🖥 שקפים` לכל אחד מ-17 המצגות (נפתח בטאב חדש).
  - **גישת עבודה שאושרה מפורשות ע"י המשתמש:** "לבנות הכל קודם, לא לפחד לתקן אחר כך" – לכן כל 17 הקבצים נבנו ברצף אחד בלי לעצור לאישור בין פאזות (בניגוד לתהליך המדורג-פאזה-אחר-פאזה שתואר במקור בברייף עצמו, סעיף 8). המשתמש אישר שגם הלומדות וגם המצגות אמורות להיבנות על בסיס מערך השיעור ולהשלים זו את זו – פער בין לומדה למערך מורה הוא ברת-תיקון בסבב הבא, לא חוסם.
  - **החלטות תוכן שהתקבלו תוך כדי בנייה (לתעד, לא לשאול שוב):** שיעור 11 – שקף העלים לא נזקק לאזכור Prospera/Valley בפועל (אין קונפליקט מתבטא בשקפים עצמם, אך `lesson-11.html` עדיין מכיל את הניסוח השגוי "Valley Irrigation" במקום "Valmont Industries/Valley הוא מותג" — לתקן בסבב הבא). שיעור 14 – דוגמת MVP (קורקינט/סקייטבורד/אופניים/מכונית) נלקחה ממערך המורה בלבד; תאי Lean Canvas בשקף נבנו לפי 3 התאים שבפועל בלומדה (בעיה/פתרון/לקוחות יעד), לא לפי הצעת מערך המורה (ערך מוצע/לקוחות/תזרים). שיעור 15 – "כשלון מהיר" נבנה כמושג רעיוני-בלבד (בלי דוגמת מוצר אמיתי בדויה, כי אין כזו במקור). שיעור 16 – נמצאה סתירה בברייף עצמו (סעיף 5 מול סעיף 7) לגבי תוכן `lesson-16-slide-3.jpg`; נפתרה ע"י בניית שקף "כרטיס-הנחיה" (כמו שיעור 10) במקום שקף תמונה קבועה, כי מערך המורה מתאר בפירוש "מורה מדגים, או סרטון" ולא תמונה סטטית.
  - **פערים שכבר זוהו ומחכים לסבב תיקונים הבא (לא טופלו עדיין):** (1) `lesson-11.html` – לתקן ניסוח Prospera/Valley. (2) טבלאות גיימיפיקציה במערכי מורה 15/16/17 לא תואמות לסכום בפועל בקוד (~70/~70/~50 מול 55/55/55). (3) בדיקת התאמה מלאה של שיעורים 1-8 בין המצגות/מערכי מורה/לומדות טרם הושלמה במלואה (subagent רץ ברקע ולא החזיר עדיין תוצאה בזמן כתיבת שורה זו).
- **שלב 9א הושלם (2026-08-05):** לפני תחילת שלב 9 בוצעה סקירת מצב מלאה מול MASTER-WORKFLOW.md. נמצא שהפרויקט **מעולם לא היה git repository** (סתירה מול "עבודה עם Git – חובה מהיום הראשון" למעלה) – זה תוקן:
  - `git init` בוצע, `user.name`/`user.email` מקומיים הוגדרו (`galil-tech` / `ben.katzir@gmail.com`, לא גלובלי).
  - `.gitignore` נוצר: קובצי נעילת Office (`~$*`), `.claude/settings.local.json`, `.claude/scheduled_tasks.lock`, ו-`lessons/aqua.mp4` (134MB, מעל מגבלת 100MB של GitHub – נותר על הדיסק, לא ב-git, עד שתיבחר שיטת אחסון: Git LFS או חיצוני).
  - Repository ציבורי נוצר ב-GitHub (`galil-tech/galil-tech`), קומיט ראשוני (131 קבצים) נדחף ל-`main`.
  - GitHub Pages הופעל, תוקן מ-build type `workflow` (דורש GitHub Actions מיותר) ל-`legacy` (פריסה ישירה מהענף). נבנה ואומת ב-curl: **https://galil-tech.github.io/galil-tech/** מחזיר 200 ומציג את `index.html`.
  - תוך כדי נוצלה ההזדמנות לסגור את 3 הפערים הפתוחים שתועדו למעלה: `lesson-11.html` תוקן (Valley → "Valmont Industries, בעלת מותג ההשקיה Valley" + תיקון typo "Prima Prospera"); `teacher-15/16/17.html` תוקנו ל-"~55 עלים" (אומת ישירות מ-`addLv()` בקוד: quiz×5 + שאר התגמולים = 55 בכל אחד מהשלושה, תואם למה שכבר תועד למעלה בשלב 6); `docs/data-schema.md` נכתב מחדש כדי לשקף את `assets/core.js` בפועל (לא את התכנון המקורי) – כולל הסרת `alv{N}` והוספת `groupLeaderboard`/`passport1`/`gc_msk`/`gc_achievements`, וסימון מפורש שעבודה-בזוגות עדיין לא באה לידי ביטוי בסכמה (שאלה פתוחה ל-9ב).
  - **לא נבדק/נפתר בסבב הזה:** פריט (3) למעלה (השוואת שיעורים 1-8 מצגות/מורה/לומדות) – עדיין פתוח.
- **שלב 9ב הושלם ואושר (2026-08-16):** `docs/data-schema.md` הורחב עם היררכיית ריבוי-מסלולים/בתי-ספר/תפקידים, ביוזמת המשתמש שביקש להוסיף לתכנון: (1) הפרדת יסודי/חטיבה ברמת מורה ותלמיד, (2) תפקיד מנהל/ת-מגמה חוצה-בתי-ספר. הכרעות עיקריות (מפורטות ב-`docs/data-schema.md` תחת "החלטות עצירה 9ב"):
  - **מסלול הוא ממד פיזי, לא פרמטר:** אין `?track=` ב-URL – "באיזה מסלול אתה" נקבע ע"י איזה אתר/דומיין (הדף הראשי מול `elementary/`, שכבר עצמאיים טכנית לפי `elementary/CLAUDE.md` #3). כניסת תלמיד נשארת `?school=&class=&student=` כמתוכנן כבר.
  - **שלושה תפקידים, לא שניים:** תלמיד/קבוצה (scope: עצמו + לוח כיתתי) → מורה (scope: כיתה/ות שהוא מלמד, בתוך מסלול אחד) → **מנהל/ת מגמה חדש** (scope: הכל, כל מסלול/בית-ספר/כיתה – תפקיד יחיד, זה המשתמש עצמו, לא רמות-ביניים בשלב זה). מורה ומנהל תלויים בפועל ב-9ד (Sheets) כדי להיות אמיתיים – 9ב רק מגדיר את ה-scope, לא בונה אותם.
  - **חוזה API משותף לבק-אנד (9ד) בין שני המסלולים** (`track`, `school_id`, `class_id`, קבוצה/תלמיד, עלים, דרכון) – רק ברמת השדות שנשלחים ל-Sheet, לא שיתוף קוד. אושר במפורש שזה לא סותר את עצמאות `elementary/assets/core.js` העתידי.
  - **עבודה בזוגות** – ההנחיה הפדגוגית הקיימת ("מחשב = תמיד בזוגות") פותרת את זה: זוג עובד ממחשב אחד משותף, לא צריך סנכרון טכני בין דפדפנים. שני שמות נכתבים כטקסט חופשי בתוך `passport1`/`studentName`.
  - כל 4 השאלות הפתוחות נענו עם ההמלצה שהוצגה (ראה תשובות מפורטות במסמך). **elementary/assets/core.js עדיין לא נבנה בפועל** – ההרחבה הזו רק שומרת מקום בסכמה, לא דורשת יישום מיידי.
- **בדיקת קישוריות מלאה (2026-08-16):** לפני 9ג הורץ סקריפט Node עצמאי שסרק את כל 84 קבצי ה-HTML בפרויקט (כולל `elementary/`, `presentations/`, `bootcamp-agtech101/`) וחילץ את כל 372 קישורי `href`/`src` מקומיים, ובדק שכל אחד מפנה לקובץ שקיים בפועל. **תוצאה: כל 372 הקישורים תקינים.** 3 "שגיאות" ראשוניות התבררו כ-false positives (טקסט הדרכה בתוך `bootcamp-agtech101/agtech101.html` שמזכיר `hero.jpg` כדוגמה, ו-template literals של JS ב-`shop.html`/`wall.html` שהרג'קס תפס בטעות כ-href אמיתי) – לא תוקן כלום, לא היה צורך.
- **שלב 9ג – התחיל (2026-08-16):** לפי ההכרעות מ-9ב (`?school=&class=&student=` בלי פרמטר `track`, שלושה תפקידים). נבנה `assets/identity.js` – מודול עצמאי (לא תלוי ב-`assets/core.js`, כי `index.html`/`wall.html`/`leaves.html`/`shop.html` לא טוענים את core.js כלל):
  - `GC_ID.ensureStudentIdentity()` – מודל כניסה (מוצג רק פעם אחת, נשמר ב-localStorage `gc_identity`): בית ספר / כיתה / שם (של התלמיד/הזוג) + "🆕 קבלת קוד חדש" (מייצר קוד 4 ספרות אקראי, מציג ב-alert) או "🔑 יש לי כבר קוד" (4 ספרות קיימות). כפתור "המשך/י בלי שמירה" לבדיקות. תג זיהוי קבוע בפינה השמאלית-תחתונה עם קישור "החלף/י" (מנקה זיהוי + טוען מחדש).
  - קורא גם פרמטרי URL (`?school=&class=&student=&name=`) – קישור ששולחת המורה מאמץ זיהוי ישירות בלי לשאול.
  - `GC_ID.ensureTeacherIdentity()` – קליל, לא חוסם: `prompt()` לבית ספר בלבד, נשמר ב-`gc_teacher_identity`, מוצג ליד "מצב מורה" בניווט.
  - **חובר לשלב זה רק ב-`index.html`** (נקודת הכניסה היחידה) – לא בעוד אף קובץ, בדיוק לפי הניסוח המפורש בעצירה 9ג ("לפני שמחברים את זה לכל 17 השיעורים").
  - **נבדק בפועל בדפדפן** (לא רק קוד): שרת סטטי מקומי + Playwright (Chromium headless, הותקן זמנית ב-scratchpad, לא בפרויקט) הריצו תרחיש מלא – כניסת תלמיד/ה חדש/ה → קבלת קוד → תג מופיע → רענון דף לא מציג את המודל שוב → לחיצה על "החלף/י" מנקה ומחזיר את המודל → כניסת מורה מציגה prompt ותג בית-ספר. **14/14 בדיקות עברו, אין שגיאות קונסולה.** צילומי מסך אומתו חזותית (RTL תקין, עיצוב תואם למותג).
- **חיבור `identity.js` לכל השאר (2026-08-16, אושר ע"י המשתמש):** סקריפט Node עצמאי חיבר את `assets/identity.js` + קריאה ל-`GC_ID.ensureStudentIdentity()` ל-21 קבצים נוספים: כל 17 `lessons/lesson-NN.html` (נתיב `../assets/identity.js`) + `wall.html`/`leaves.html`/`shop.html`/`journey.html` (נתיב `assets/identity.js`). ההתנהגות: אם זהות כבר נשמרה (למשל מ-`index.html`) – רק תג הזיהוי מוצג, בלי למודל להופיע שוב; אם אין זהות (כניסה ישירה לשיעור בלי לעבור דרך `index.html`) – המודל מופיע שם.
  - **נבדק שוב end-to-end עם Playwright:** קדם-מילוי זהות (מדמה כניסה קודמת מ-`index.html`) → ניווט ל-6 דפים שונים (`lesson-05`, `lesson-17`, `wall`, `leaves`, `shop`, `journey`) – בכולם המודל נשאר סמוי והתג מוצג נכון. בנוסף: כניסה ישירה לדף שיעור *ללא* זיהוי קודם (`lesson-03`) – המודל כן מופיע, מעל תוכן השיעור, בלי לשבור את הפריסה. **13/13 בדיקות עברו.** שגיאות הקונסולה היחידות שנרשמו (4 בקשות 403 ב-`wall.html`) הן קישורי דמו חיצוניים ל-imgur שכבר קיימים בקוד מלפני השינוי (נתוני placeholder) – לא קשור לשינוי הזה, לא תוקן.
  - **בדיקת קישוריות חוזרת:** 394 קישורים מקומיים (עלה מ-372 בעקבות 21 תגי script חדשים) – עדיין 0 קישורים שבורים אמיתיים.
  - נדחף ל-git.
- **עצירה 9ג הושלמה במלואה.**
- **שלב 9ד – התחיל (2026-08-16):** Google Sheets + Apps Script, לפי הסכמה מ-9ב. **חלוקת עבודה חשובה לזכור:** ל-Claude Code אין גישה לחשבון Google של המשתמש (אין `clasp`/`gcloud` מותקנים/מאומתים בסביבה) – פריסת ה-Apps Script בפועל היא **פעולה חד-פעמית שרק המשתמש יכול לבצע** (התחברות לחשבון Google שלו). מה שנבנה בצד הקוד:
  - **`data/apps-script/Code.gs`** – קובץ מקור מלא (עותק לגיט, לא רץ מ-GitHub Pages) עם `doPost`/`doGet` לפי החוזה מ-9ב: `track/school_id/class_id/group_name/student_name/code/ls1..17/leaves_spent/passport1/achievements/last_updated`. Upsert לפי מפתח (`track+school_id+class_id+code`) – לא יוצר שורה כפולה לאותו תלמיד. הגנה בסיסית (לא אבטחה אמיתית) עם `SHARED_SECRET`. הוראות פריסה מלאות בראש הקובץ עצמו.
  - **`data/sheets-config.js`** – קונפיג צד-לקוח, `enabled:false` כברירת מחדל (שום דבר לא נשלח החוצה עד שממלאים `url`+`token` בפועל).
  - **`assets/sheets-sync.js`** – מודול חדש: אוסף את כל הנתונים הרלוונטיים מ-localStorage + זהות (`GC_ID`), שולח ל-Apps Script עם debounce של 3 שניות (לא מציף בכל תשובת quiz בודדת), ועוד ניסיון סנכרון אחרון ב-`pagehide` דרך `sendBeacon`.
  - **חובר ב-`assets/core.js`** (לא בכל קובץ שיעור בנפרד): קריאה ל-`GC_SYNC.scheduleSync()` נוספה בתוך `GC.addLeaves`, `GC.savePassport`, `GC.pushPassport` – כלומר כל 17 הלומדות מסונכרנות אוטומטית ברגע ש-Sheets יופעל, בלי לגעת בקבצי השיעור עצמם.
  - **חובר ב-17 קבצי הלומדה:** תגי script ל-`data/sheets-config.js` + `assets/sheets-sync.js` (מאותה סיבה טכנית שהצריכה גם את `identity.js` בכל קובץ בנפרד ב-9ג – core.js לא נטען בכל דף).
  - **תיקון אגב (נמצא תוך כדי בדיקה, לא קשור ל-9ד ישירות):** `assets/identity.js` זרק שגיאת קונסולה לא-תפוסה (`Cannot read properties of null (reading 'appendChild')`) בכל טעינת דף עבור תלמיד/ה חוזר/ת (זהות כבר שמורה) – כי `renderBadge()` נקרא לפעמים מתוך `<head>` לפני שה-`<body>` קיים. לא השפיע על התג בפועל (מנגנון גיבוי ב-DOMContentLoaded תמיד תיקן את זה בשקט), אבל תוקן כראוי עם guard `if (!document.body) return;`.
  - **נבדק במלואו מקומית עם שרת Apps Script מדומה** (Node, חוזה זהה בדיוק ל-`Code.gs` האמיתי) + Playwright: זהות → `GC.addLeaves(5)` אמיתי → debounce מחזיק (0 POSTs תוך 500ms) → POST יחיד אחרי 3 שניות → השורה חוזרת נכון מ-GET עם כל השדות (כולל `token` לא דלף) → 3 קריאות addLeaves רצופות מתמזגות ל-POST יחיד עם הסכום המצטבר. **11/11 בדיקות עברו.** נבדק גם שהקונפיג המחובר בפועל (`enabled:false`) גורם ל-no-op בטוח לגמרי, בלי קריאת רשת.
- **שלב 9ד הושלם ואומת במלואו על התשתית האמיתית (2026-08-16):**
  - המשתמש פרס את `Code.gs` בעצמו (חובר URL: `script.google.com/macros/s/AKfycbwZ1XmZKau7nylCd7YxkKVjMGnQpcvtm9b9Odoja6uIFbve9vebId5RIQ58g0RIt4Q/exec`). בדרך נתקלנו ותוקנו שתי תקלות פריסה נפוצות: (1) "הגישה נדחתה" – הפריסה הראשונה לא הוגדרה עם Who has access=Anyone; (2) `SHARED_SECRET` חזר לברירת המחדל `CHANGE_ME_2026` כי מחיקת-והדבקת-כל-הקובץ מחדש (כדי לעדכן ל-doGet עם טוקן) דרסה גם את השינוי הידני של המשתמש לסוד – שוחזר.
  - **החלטת אבטחה (נשאלה מפורשות, לא הוכרעה לבד):** הריפו ציבורי, ולכן ה-URL וה-`SHARED_SECRET` יהיו גלויים לכל מי שקורא את קוד הריפו ברגע שיוכנסו ל-`data/sheets-config.js`. חמור יותר: ה-`doGet` המקורי לא בדק טוקן בכלל – כל מי שהיה נתקל ב-URL (מטבעו ציבורי, כי קוד צד-לקוח חייב אותו) היה יכול לקרוא את נתוני **כל** התלמידים בלי הרשאה. המשתמש בחר להוסיף בדיקת טוקן גם ל-`doGet` (שיפור מוגבל – לא פותר מי שקורא את הריפו הציבורי עצמו, רק חוסם גישה מזדמנת/סריקה אוטומטית של מי שנתקל ב-URL בפני עצמו). זו הכרעה מודעת, לא סופית – שיפור הרשאות אמיתי (Google Sheets לא-ציבורי לגמרי + הרשאות אמיתיות) הוא נושא ל-9ו/הקשחה עתידית.
  - **באג אמיתי שנמצא ותוקן דרך הבדיקה (לא רק תכנון):** `assets/sheets-sync.js` – ה-handler של `pagehide` שלח תמיד beacon עם המצב הנוכחי בכל עזיבת דף, גם כשלא היה שינוי אמיתי ממתין. תרחיש קונקרטי שנתפס: שמירת זהות → `reload()` → beacon נשלח עם `ls7:0` (עוד לפני שנלמד משהו) → הגיע לשרת **אחרי** POST תקין עם `ls7:7` (sendBeacon לא מבטיח סדר הגעה) → "כתיבה אחרונה מנצחת" דרס נתון טוב בנתון ישן. תוקן: `pagehide` שולח רק אם `syncTimer` פעיל בפועל (משהו כבר ממתין), ומבטל את הטיימר המקורי כדי שלא יישלחו שתי בקשות מתחרות. אומת מחדש מול שרת מדומה (11/11) ומול Google Sheet אמיתי.
  - **גילוי אדריכלי חשוב:** רק `lesson-01.html` קורא בפועל ל-`GC.addLeaves` של `core.js` – שאר 16 הלומדות (02–17) משתמשות בפונקציית `addLv()` מקומית משלהן שכותבת ישירות ל-`localStorage`, ולא עוברות דרך `core.js` בכלל (תיעוד מדויק יותר ממה שנרשם קודם בשלב 6 – "core.js נוסף כ-script tag" לא אמר שהלוגיקה הפנימית הועברה אליו). המשמעות: החיבור המקורי של `GC_SYNC.scheduleSync()` בתוך `GC.addLeaves`/`savePassport`/`pushPassport` ב-`core.js` **לא הספיק** – התווספה קריאת `GC_SYNC.scheduleSync()` ישירות בתוך פונקציית ה-`addLv()` המקומית של כל אחת מ-16 הלומדות (חיבור מבוסס-regex על `localStorage.setItem('lsN', lv);` שכל 16 הקבצים חולקים, בדיוק לפני `document.getElementById('lc')...`), כדי שסנכרון יקרה בפועל בכל 17 השיעורים, לא רק בשיעור 1.
  - **אומת end-to-end על התשתית האמיתית** (לא מדומה): שרת סטטי מקומי מגיש את הקבצים המחוברים בפועל + Chromium (Playwright) פותח `lesson-07.html`, קובע זהות, קורא ל-`addLv(7)` (בדיוק כמו לחיצה אמיתית על תשובת quiz), וממתין לסנכרון הטבעי (debounce 3 שניות) – הנתון נחת בגיליון האמיתי של המשתמש עם כל השדות נכונים (`ls7:7`, זהות, `track:"middle"`). **הערת בדיקה:** רשת יוצאת ל-Google מהסביבה הזו לפעמים איטית/משתנה (נצפו גם timeouts זמניים בשלבים קודמים מול GitHub API) – ניסיונות ראשונים עם חלון-המתנה קצר מדי נראו כמו "כשל", אך התבררו כאיטיות רשת בלבד, לא באג; הבדיקה הסופית עם המתנה ארוכה יותר עברה בבירור.
  - `data/sheets-config.js` מכיל כעת את ה-URL וה-token האמיתיים, `enabled:true` – **הסנכרון פעיל בפועל באתר החי**.
  - **שורות בדיקה שנותרו בגיליון האמיתי** (לא נמחקו אוטומטית – אין endpoint למחיקה ב-`Code.gs` בכוונה, לא נדרש לשלב זה): `school_id`/`class_id` עם הערכים `__TEST__`, `__E2E_LIVE__`, `__E2E_LIVE_V2__`, `__E2E_LIVE_V3__`, `__DEBUG_UPSERT__`, `__DBG_FETCH__`, `__E2E_FINAL__`, `y`/`y` (כולן מסומנות בבירור בשם "בדיקת.../dbg" ב-`student_name`) – מומלץ שהמשתמש ימחק אותן ידנית מהגיליון לפני שמתחילים להשתמש בו עם תלמידים אמיתיים.
- **עצירה 9ד הושלמה במלואה.**
- **שלב 9ה הושלם (2026-08-16):** חיבור ההרשאות מ-9ג לנתונים האמיתיים מ-9ד. מה נבנה:
  - **`data/apps-script/Code.gs`** – נוסף גיליון שני `groups` (נפרד מ-`students`, כי לוח מנהיגות הוא לפי קבוצת-פרויקט לא לפי תלמיד): `doGet(?type=groups&school=&class=)` מחזיר קבוצות, `doPost({type:'group', addLeaves})` מוסיף לסכום קיים (לא דורס) — תואם בדיוק את מנגנון "הוסף/עדכן קבוצה" הידני שכבר קיים ב-`leaves.html`. גם `doGet` הרגיל (תלמידים) קיבל פילטר `&code=` לשליפת שורה אישית בודדת.
  - **`assets/sheets-sync.js`** – שלוש פונקציות חדשות: `pullMine()` (מושכת את השורה האישית, ממזגת ל-localStorage לפי **מקסימום** פר-שיעור כדי שהתקדמות אף פעם לא נסוגה אחורה בין מכשירים, ומאמצת `passport1`/`achievements` מהגיליון רק אם המקומי עדיין ריק), `pullGroups()` (לוח מנהיגות משותף אמיתי), `pushGroup()`. הידרציה אוטומטית-שקטה (`pullMine()`) בכל טעינת דף שבו מזוהה זהות.
  - **חובר ל-`journey.html`/`shop.html`/`leaves.html`** (לא ל-`wall.html` — נבדק שהוא לא נוגע בכלל בנתוני עלים, אין מה להדרג שם). `leaves.html` עודכן כך שהלוח בין-קבוצתי נמשך מהגיליון בפועל כשסנכרון פעיל, עם נפילה חזרה ל-localStorage אם לא (למשל למסלול היסודי לפני שיהיה לו core.js משלו).
  - **באג אמיתי נוסף שנמצא ותוקן דרך הבדיקה (לא קשור ל-9ה ישירות, אך התגלה רק בזכות הבדיקה החדשה מול Sheet אמיתי):** ב-`lesson-05.html`, סליידר ה-EC (`updateEC`) מוגדר עם ערך ברירת מחדל (16 → EC=1.6) שכבר נופל בטווח ה"מנצח" (1.2–2.0), וקריאת האתחול `updateEC(16)` בתחתית הקובץ הפעילה את בדיקת הניצחון החד-פעמית **בכל טעינה/רענון של הדף**, מעניקה 15+ עלים אוטומטית בלי שום אינטראקציה אמיתית של התלמיד (וגדל ללא הגבלה בכל רענון נוסף). נבדקו גם lesson-06/07 (אותו פטרן סליידר) — שם ה**לא** קיים הבאג (lesson-06 לא קורא לפונקציה באתחול; lesson-07 מעניק בונוס רק על מצב "רע" שברירת המחדל לא נמצאת בו). תוקן: הוספת פרמטר `isInit` ל-`updatePH`/`updateEC` שמדלג על בדיקת הניצחון בקריאת האתחול בלבד.
  - **נבדק end-to-end עם "שני תלמידים מדומים מאותה כיתה"** (Playwright, שרת מדומה תחילה ואז מול ה-Sheet האמיתי): (1) כל תלמיד/ה רואה רק את הסכום האישי שלו/ה, לא של השני/ה. (2) שני התלמידים שמוסיפים לאותה קבוצה בלוח המנהיגות רואים את אותו סכום משותף (5+7=12), לא רק את התרומה האישית שלהם. (3) תלמיד/ה שפותח/ת "מכשיר חדש" (localStorage ריק, אותה זהות) רואה נכון את ההתקדמות האמיתית שלו/ה, שנמשכה מהגיליון. **כל הבדיקות עברו** אחרי תיקון באג ה-EC.
  - **המשתמש צריך לפרוס מחדש את `Code.gs` המעודכן** (Deploy → Manage deployments → New version) לפני שהתכונות החדשות (לוח מנהיגות משותף, פילטר `code`) יעבדו בפועל באתר החי — **לזכור לשמור את `SHARED_SECRET` בפעם הזו** (זו הייתה תקלה בפעם הקודמת, ראה יומן 9ד).
- **עצירה 9ה הושלמה ואושרה במלואה על הגיליון האמיתי (2026-08-16):** אחרי הפריסה השנייה של המשתמש, נמצא ותוקן עוד באג אמיתי דרך הבדיקה החיה: **מירוץ תנאים ב-`doPost`** — שני תלמידים שמוסיפים לאותה קבוצה כמעט בו-זמנית (התרחיש בדיוק שהעצירה דורשת לבדוק!) יצרו שתי שורות נפרדות בגיליון `groups` (4+3) במקום שורה אחת מעודכנת (7), כי שני ה-doPost קראו את הגיליון לפני ששניהם הספיקו לכתוב. תוקן עם `LockService.getScriptLock()` סביב כל `doPost` (מסחרר גם את עדכוני התלמידים, לא רק קבוצות). המשתמש פרס מחדש בפעם השלישית (עם תזכורת לשמור את `SHARED_SECRET`), ומחק ידנית את שורות הבדיקה הישנות.
  - **9/9 בדיקות עברו על הגיליון האמיתי** אחרי התיקון: שתי שורות תלמידים נחתו מבודדות עם הערכים הנכונים; שני תלמידים שמוסיפים לאותה קבוצה **כמעט בו-זמנית** רואים סכום משותף נכון (3+4=7, לא כפילות) בשני הדפדפנים; מכשיר חדש עם אותה זהות משוחזר נכון מהגיליון. **גילוי חשוב מהבדיקות:** בסביבת הבדיקה שלי לפעמים לוקח כ-5 שניות עד שנתון מהגיליון מגיע בפועל לדפדפן (רשת יוצאת לגוגל מהסביבה הזו לפעמים איטית, נצפה כמה פעמים לאורך שלבי 9ד-9ה) – לא באג במוצר, אבל שווה לזכור שזו לא בהכרח חוויית התלמידים בכיתה עם אינטרנט רגיל.
- **בקרת איכות כוללת מוקדמת (2026-08-17/18, ביוזמת המשתמש – לא חלק משלב 9 הרשמי):** לפני 9ו, המשתמש ביקש `/expert-panel` על **כל הפרויקט** (כמו שלב 10, אך מוקדם). הופעלו יובל+ענת+נועה+אורי+דנה במקביל. **תקלה טכנית משמעותית בסבב הזה:** נועה (גיימיפיקציה) נכשלה **5 פעמים ברצף** ("stalled: no progress for 600s") גם עם היקף משימה מינימלי בן קובץ אחד – תקלת סביבה, לא קשורה לתוכן. מיכל (הסינתזה) גם נכשלה פעם אחת ("Connection lost") ועברה בהצלחה בניסיון שני. במקום דוח נועה, ביצעתי בעצמי בדיקה ישירה וממוקדת (ראה למטה).
  - **ממצאי יובל (פדגוגיה) – 2 קריטיים:** שרשרת הטעאזר שבורה לגמרי בגבול lesson-04→05 (lesson-04 מבטיח טעאזר מפורש, lesson-05 פותח בלי גשר) ובגבול lesson-12→13 (התברר בבדיקה שלי שהטעאזר **כן** קיים בסוף lesson-12 בפועל – הממצא המדויק היה שחסר גשר-מקביל בפתיחת lesson-13). גם: שיעורים 5/6/7 חסרים `textarea` פעיל לכוכב הצפון (קיים ב-14 מתוך 17 השיעורים האחרים), ומודל הזהות (`identity.js`) יוצר רצף "ברוכים הבאים" כפול בכניסה ל-lesson-01 (לא טופל בסבב הזה – נשאר פתוח, ראה למטה).
  - **ממצאי ענת (מעשיות) – טופלו באותה שיחה, לפני הסינתזה:** תיעוד פרמטרי URL לדילוג על מסך הזהות + כפתור דילוג בולט יותר, תיקון "מחשב/טאבלט לכל תלמיד"→"לכל זוג" ותגית "עצמאי"→"זוגות" ב-`teacher-05/06.html` (סתרו את "עיקרון העבודה בכיתה"), הוספת תגית הרכב-עבודה חסרה ב-`teacher-07.html`, השלמת קוד ארדואינו לדוגמה חסר-חלקים (`setup()`/`pinMode`/`Servo`) ב-`teacher-08.html`.
  - **ממצאי אורי (סטארטאפים):** עקביות Prospera/Valmont/Valley תקינה לגמרי בין lesson-11/teacher-11. ממצא קל בלבד: `teacher-11.html` מכיל פרט "$300M, 2021" שלא מאומת/מתועד במקום אחר – פתוח, לא דחוף.
  - **ממצאי דנה (תשתית) – 2 קריטיים חמורים בשכבת ה-Sheets:** (1) `doPost` לשורת תלמיד עשה **דריסה עיוורת מלאה** (בניגוד ל-`groups` שכבר עשה max-merge נכון) – מכשיר "מפגר" (לא מסונכרן) יכול היה לאפס בטעות התקדמות אמיתית שכבר עודכנה ממכשיר אחר של אותו תלמיד/זוג. (2) `doGet` בלי דרישת פרמטר `code` החזיר **dump מלא של כל התלמידים בכל בתי הספר** (כולל דרכון+הישגים) לכל מי שקורא את ה-URL הציבורי (שיושב גלוי, יחד עם ה-SHARED_SECRET, ב-`data/sheets-config.js`). גם: קוד 4 הספרות (`rand4()`) מיוצר בלי בדיקת ייחודיות מול השרת – סיכוי לא-זניח (בעיית יום-הולדת, כיתה של 30) לשני תלמידים עם אותו קוד ש"חולקים" בטעות אותה שורה. מיזוג `passport1`/הישגים ב-`pullMine` הוא all-or-nothing, לא per-key.
  - **הבדיקה שלי במקום נועה:** אישרתי ישירות (חוזר על מה שכבר נבדק בשלב 9ה) ש-`lesson-06.html` לא קורא ל-`updateSensors()` באתחול הדף בכלל, ו-`lesson-07.html` מעניק בונוס רק על מצב "רע" שברירת המחדל של הסליידרים לא נמצאת בו – כלומר באג "עלים אוטומטיים מהאתחול" (שתוקן ב-lesson-05) הוא **מקומי ללא-05 בלבד**, לא דפוס שיטתי.
  - **מיכל סימנה דפוס חוצה-דוחות חשוב:** אי אפשר להעריך שאלות עיצוב על לוח המנהיגות המשותף (למשל תחרותיות שלילית) כל עוד הנתונים שהוא מציג עלולים להיות שגויים בגלל באג הדריסה של דנה – קודם אמינות נתונים, אחר כך עיצוב.
- **תשובות המשתמש + יישום (2026-08-18/19):**
  1. **doGet הפתוח:** "אין כרגע תלמידים אמיתיים רק בדיקות. האם ניתן שיכנסו עם שם פרטי ו-4 ספרות? אם זה מסבך אין צורך" – תוקן: `doGet` ל-`Code.gs` דורש עכשיו `code` חובה (שגיאה בלעדיו) ומסנן מיד לפי קוד, לא רק בסוף. גם `doPost` לשורת תלמיד תוקן למיזוג per-field בסגנון `Math.max` לכל `ls{N}`/`leaves_spent` (כמו ב-`groups`), ושמירת `passport1`/הישגים קיימים אם המגיע ריק – סוגר גם את ממצא הדריסה העיוורת. **שדה השם במודל הכניסה (`identity.js`) פושט לשם פרטי בלבד** ("שם פרטי (שלכם, או שם פרטי אחד מבני הזוג)" + הערת "הקוד האישי הוא מה שבאמת מזהה אתכם") – מפחית רגישות המידע הנשמר, לא דרש שינוי מבני. **בדיקת ייחודיות קוד לא יושמה** (המשתמש נתן פתח לדלג אם מסבך – זה כן מסבך בארכיטקטורה סטטית-בלי-מצב-שרת-בזמן-כניסה, נשאר פתוח).
  2. **לוח מנהיגות:** "דירוג מלא" – אין שינוי, ההתנהגות הקיימת כבר נכונה.
  3. **שיעורים 5-7:** "יישר אותם" – בוצע לפי הממצאים הספציפיים שעלו (לא שוקלל מעבר ארכיטקטוני מלא ל-`GC.addLeaves`, כי הסנכרון כבר עובד נכון דרך `addLv` המקומי בכל השיעורים 2-17, לא רק 5-7 – זה לא "חוסר יישור" אמיתי, זו הארכיטקטורה הקיימת בכל הפרויקט): נוסף `textarea#north-star` פעיל ל-lesson-05/06/07 (תבנית זהה ל-14 השיעורים האחרים), נוסף גשר-טעאזר תואם ב-lesson-05 (מצטט את הטעאזר בפועל מ-lesson-04) ובגשר לב-lesson-13 (מצטט את הטעאזר בפועל מ-lesson-12).
  - **נבדק:** לינק-צ'ק מלא (0 שבורים), רגרסיה מלאה מול שרת מדומה מעודכן (11 בדיקות ישנות + בדיקה חדשה ייעודית לתרחיש "מכשיר מפגר" שדנה תיארה – 6/6 עברו, כולל אימות ש-`doGet` בלי `code` נדחה).
  - **אומת על הגיליון האמיתי (2026-08-19), אחרי פריסה מחדש רביעית של המשתמש:** (1) `doGet` בלי `code` נדחה עם `{"ok":false,"error":"code parameter required"}` – אושר. (2) תרחיש "מכשיר מפגר" נבדק ישירות מול השרת האמיתי: מכשיר טרי כתב `ls5=20`, מכשיר "מפגר" (סימולציה) שלח `ls5=5` + `passport1` ריק – השרת **שמר על `ls5=20`** (לא נסוג אחורה) **ושמר על `passport1` הקיים** (לא נדרס בערך ריק). שני התיקונים הקריטיים של דנה מאומתים במלואם על התשתית האמיתית, לא רק במוק.
  - **פערים שנותרו פתוחים במודע, לא נדרשו בסבב הזה:** רצף "ברוכים הבאים" כפול ב-identity.js (יובל), בדיקת ייחודיות קוד 4 ספרות (דנה), מיזוג per-key ל-passport1/הישגים במקום all-or-nothing (כבר יש הגנה בסיסית all-or-nothing, לא מיזוג עדין יותר – דנה), אימות הפרט "$300M" ב-teacher-11 (אורי).
- **עצירה 9ה + בקרת האיכות המוקדמת נסגרו במלואן ואומתו על התשתית האמיתית.** נקודת עצירה טבעית לסיום שיחה, לפי "פרוטוקול שיחה חדשה לכל נושא" ב-MASTER-WORKFLOW.md.
- **שלב 9ו (2026-08-19) – נבנה ונבדק במלואו, ממתין לאישור סופי של המשתמש:** לפי החזון ב-CLAUDE.md ("ממשק רב-כיתתי ובין-בית-ספרי"), נבנו שלושת המרכיבים שנותרו משלב 9 + ממשק מנהל/ת-מגמה שהוגדר ב-9ב:
  - **החלטות עיצוב שהתקבלו מהמשתמש לפני הבנייה:** (1) הזדהות מנהל/ת-מגמה = סיסמה קבועה בקוד הצד-לקוח (לא טוקן ב-URL, לא בלי הגנה כלל) – תואם את פשרת האבטחה הקיימת כבר בפרויקט (ריפו ציבורי, אין הגנה אמיתית, רק חסם-כניסה מזדמן לתפקיד יחיד שהוא המשתמש עצמו). (2) גלריית אבטיפוסים = קישור לתמונה חיצונית (Google Drive/Photos), לא אחסון קבצים באתר (סטטי, GitHub Pages, אין אחסון תמונות אמיתי).
  - **`data/apps-script/Code.gs`** – נוסף גיליון שלישי `gallery` (id/school/class/group/title/image_url/description/votes/submitted_at). Endpoints חדשים: `doGet?type=heights` (לוח "מי הצמיח הכי גבוה" גלוי-לכולם – מחשב מקסימום גובה מתוך `passport1` של כל שורה בפונקציה `_maxHeightFromPassport`, סורק `lesson4.height`/`measurements[].height`/`growthData[].height`, מחזיר שדות מצומצמים בלבד – לא code/passport מלא/הישגים, כי זה עמוד ציבורי), `doGet?type=gallery` (ציבורי, גלוי לכולם), `doGet?type=admin_students` (dump מלא של כל התלמידים+קבוצות מכל בית ספר/מסלול – רק לממשק המנהל/ת), `doPost` עם `type=gallery_submit`/`gallery_vote`/`gallery_delete` (מחיקה למנהל/ת בלבד, לצורך ניהול תוכן שמגיע מטופס הגשה ציבורי).
  - **`assets/sheets-sync.js`** – נוספו `GC_SYNC.pullHeights/pullGallery/submitGallery/voteGallery/pullAdminAll/deleteGalleryItem`.
  - **`cross-school.html`** (חדש, שורש) – לוח "מי הצמיח הכי גבוה" בין-בית-ספרי + סיכום שיא-גובה לפי בית ספר. עודכן בזמן אמת (לא טקס העלאה שבועית פורמלי כמו שנוסח בחזון המקורי – פשוט יותר, מתאים לארכיטקטורת הסנכרון הרציף הקיימת כבר).
  - **`gallery.html`** (חדש, שורש) – טופס הגשה (שם+קישור תמונה+תיאור) + גלריית כרטיסים עם הצבעה (הצבעה אחת לדפדפן, נאכף ב-localStorage).
  - **`admin-dashboard.html`** (חדש, שורש) – שער סיסמה (`ADMIN_PASSWORD` קבוע בקוד, ברירת מחדל `galil2026admin` – ניתן לשינוי), לאחר כניסה: סיכום לפי בית ספר, טבלת כל התלמידים/קבוצות מכל בתי הספר, טבלת כל קבוצות הפרויקט, וניהול/מחיקת פריטי גלריה בעייתיים.
  - **`index.html`** – קישורי ניווט חדשים ל-3 העמודים (תפריט תלמיד + סקציית "כלים למדריך").
  - **בדיקות:** (1) בדיקת קישוריות מלאה – 0 שבורים אמיתיים (458 קישורים נסרקו). (2) בדיקה מלאה מול שרת מדומה (Playwright, שתי "כיתות"/שני "בתי ספר" מדומים) – **18/18 עברו**: לוח גבהים ממוין נכון עם סיכום לפי בית ספר, הגשה+הצבעה בגלריה חוצת-בתי-ספר עם אכיפת הצבעה-יחידה, שער סיסמת מנהל (דחיית סיסמה שגויה + קבלת סיסמה נכונה), ראיית-הכל של המנהל/ת על שני בתי הספר, מחיקת פריט גלריה, התמדת session בין רענונים.
  - **פריסה מחדש (פעם שישית) + בדיקה על הגיליון האמיתי:** בסבב הראשון `SHARED_SECRET` התאפס שוב לברירת המחדל (אותה תקלה חוזרת מ-9ד – נדרש תיקון+פריסה נוספת). אחרי התיקון: אומת ישירות (curl + Playwright) שכל שלושת ה-endpoint-ים החדשים עובדים על הגיליון האמיתי – שני תלמידי-בדיקה משני "בתי ספר" (`__9v_LIVE_A__`/`__9v_LIVE_B__`) הופיעו נכון בלוח הגבהים (77/55 ס"מ, ממוין), שתי הגשות גלריה משני בתי הספר נראו זו לצד זו, הצבעה+מחיקה בוצעו בהצלחה (אומת ישירות מול מצב הגיליון הסופי, לא רק מקוד התגובה), ומנהל/ת המגמה ראה/תה את שני בתי הספר יחד. **הערת בדיקה:** ריצת Playwright הראשונה מול הגיליון האמיתי דיווחה 5 כשלים שגויים (false negatives) בגלל תזמון קשיח מדי מול חביון-רשת משתנה של גוגל (תופעה שכבר תועדה כמה פעמים ב-9ד/9ה) – אומת בנפרד מול הגיליון עצמו שהנתונים בכל זאת נחתו נכון. גם `curl -L` על POST הפיק עמוד שגיאה 411 מטעה (חוסר Content-Length אחרי הפניית 302 של Apps Script) – זו תקלת-לקוח של curl, לא כשל שרת; אומת שהמוטציה בכל זאת בוצעה (בדיקת מצב-גיליון לפני/אחרי).
  - **שורות בדיקה שנותרו בגיליון (לניקוי ידני אופציונלי, לא דחוף):** `students`: `__9v_LIVE_A__`/`__9v_LIVE_B__` (בנוסף לרשימת שורות-הבדיקה הישנות מ-9ד/9ה שכבר תועדה שם ועדיין לא נוקתה). `gallery`: נוקה במלואו בתוך הבדיקה עצמה (שני הפריטים נמחקו דרך `admin-dashboard.html`/API).
  - **ממתין לאישור מפורש של המשתמש** על עצירה 9ו לפני שנחשב שלב 9 כולו (9א–9ו) לסגור ועוברים לשלב 10 (בקרת איכות כוללת, השלב האחרון בתוכנית כולה).
- **בקשות המשך ישירות מהמשתמש אחרי 9ו (2026-08-19, לא חלק ממוספר מ-MASTER-WORKFLOW, נבנו ונבדקו באותה שיחה):**
  1. **סיסמת מורה פשוטה** (`index.html`) – `TEACHER_PASSWORD` קבוע בקוד (`moradaat2026`), נבדק מול הכפתור "👨‍🏫 מורים" בלבד (המשתמש בחר במפורש **לא** לגדר גם כל קובץ `teachers/teacher-NN.html` בנפרד – כניסה ישירה ל-URL של קובץ מורה עדיין פתוחה כמו קודם, היקף מצומצם במודע). נשמר ב-`sessionStorage['gc_teacher_pw_ok']` כדי לא לשאול שוב באותו סשן.
  2. **פתיחת שיעורים הדרגתית** – "סיים שיעור" הוגדר (לבקשת המשתמש) כ"הגיע לשלב הסיכום הסופי (כוכב הצפון)". יושם **גנרית ב-`assets/core.js`** (לא בכל קובץ שיעור בנפרד): גילינו תוך כדי שרק `lesson-01.html` באמת משתמש ב-`GC.goTo`/מצב פנימי של core.js – שאר 16 השיעורים מגדירים `goTo()` מקומי משלהם שדורס את זה (עקבי עם הממצא הקודם מ-9ד לגבי `addLv`). הפתרון: `MutationObserver` גנרי שצופה במחלקת `.active` על אלמנט ה-`step-panel` בעל המספר הגבוה ביותר בדף (הקונבנציה היחידה שבאמת משותפת לכל 17 – `id="step-N"`), וכשהוא נהיה פעיל שומר `localStorage['lesson{N}_completed']='1'`. אומת שעובד גם על lesson-01 (GC.goTo) וגם על lesson-05 (goTo מקומי, 7 שלבים) – שני מסלולי הניווט השונים.
     - `index.html`: רשימת השיעורים במצב תלמיד (שהייתה **תקועה/לא מעודכנת מאז שלב 6** – שיעורים 1-4 היו `href="#"` לא-קליקביליים, ושיעורים 8-17 היו placeholder "בקרוב/נעול" קבוע, למרות שכל 17 השיעורים כבר בנויים בפועל) נבנתה מחדש כרשימה דינמית (`renderStudentLessons()`, מערך `LESSONS`): שיעור 1 תמיד פתוח, שיעור N פתוח רק אם `lesson{N-1}_completed`, כרטיס נעול מציג 🔒 + "סיימו את שיעור N-1" בלי קישור אמיתי.
  3. **תלמידים רואים רק לומדה + מצגת, לא מערך מורה** – נבדק שזה כבר היה נכון בפועל (טבלת מערכי המורה כבר בתוך `teacher-only`, וללומדות אין קישור למערכי מורה בכלל). מה שהיה חסר ונוסף: קישור "🖥 שקפים" (`presentations/lesson-NN-slides.html`) לצד כל כרטיס שיעור **פתוח** בתצוגת התלמיד (לא היה שם בכלל קודם – מצגות היו זמינות רק במצב מורה).
  - **בדיקות:** Playwright, 12/12 עברו – שער סיסמה (דחיית סיסמה שגויה, קבלת סיסמה נכונה, התמדת session), שיעור 1 פתוח כברירת מחדל, שיעור 2 נעול (בלי קישור אמיתי בדף), קישור שקפים מוצג, השלמת שיעור 1 (`GC.goTo(6)`) פותחת שיעור 2 ומשאירה שיעור 3 נעול, הודעת התקדמות מעודכנת. בנפרד אומת גם על lesson-05 (goTo מקומי) שהדגל נשמר נכון. בדיקת קישוריות מלאה – 0 שבורים אמיתיים.
- **שלב 10 – בקרת איכות כוללת סופית (2026-08-19), בוצע ואושר במלואו:** לפי MASTER-WORKFLOW.md, לפני שלב 10 קודם בוצע commit של עבודת שלב 9ו שהמתינה (אישור מלא של המשתמש התקבל דרך המשך ישיר לשלב 10). הופעל `/expert-panel` **על כל הפרויקט** (לא קובץ בודד) – יובל+ענת+נועה+אורי+דנה במקביל, כל אחד עם היקף ממוקד, ואז מיכל סינתזה (נכשלה פעמיים ברצף מ-"Connection lost"/"response stopped arriving" לפני שהצליחה בניסיון שלישי עם תמצית מקוצרת – תואם לתקלות דומות שכבר תועדו ב-9ה).
  - **ממצאים קריטיים שנמצאו ותוקנו (כולם אושרו ע"י המשתמש: "אישור לתקן את כל ההצעות הקונקרטיות" + 3 הכרעות scope נפרדות):**
    1. **בטיחות (ענת) – הכי דחוף:** `teacher-05.html` הנחה להוסיף pH UP/DOWN (חומרים קאוסטיים/חומציים) בלי אזהרה לבני 14; `teacher-14/15.html` – דבק חם (120-180°C) בלי אזהרת כוויות. תוקן בשלושתם (אזהרות מפורשות בעיצוב עקבי לקובץ).
    2. **שרשרת טעאזר שבורה (יובל):** lesson-03→04 (הבטחה משוכפלת/שגויה – טעאזר של שיעור 5 הופיע בטעות בסוף שיעור 3), lesson-14→15 (גבול שבור לגמרי), lesson-16→17 (חסר בשני הכיוונים). כולם תוקנו + תיקון קוסמטי לגבול 05→06 + חגיגת מעבר פאזה 2→3 בשיעור 9 (עקביות מול פאזות 1→2/3→4).
    3. **מנוע גיימיפיקציה Phase 2 (נועה) – פועל רק ב-lesson-01, לא ב-16 השיעורים האחרים:** המשתמש הכריע (אחרי שהוצגו 2 אופציות) על **הרפקטור המלא** – "לשחזר PlantTwin/קומבו/הישגים/רצף/סאונד בכל 16 השיעורים", לא רק לתעד את הפער. **בוצע** (ראה פירוט טכני למטה).
    4. **בנק שאלות בונוס מת (נועה):** קוד קיים ב-`questions.js` אך 0 קריאות בפועל, כש-teacher-05/06/07 ו-`GC.LESSON_MAX` הצהירו על 35 עלים לא-ניתנים-להשגה. המשתמש הכריע: **לחבר בפועל** (לא למחוק). **בוצע** – `GC.injectBonusQuestions` מחובר עכשיו ב-lesson-05/06/07.
    5. **תשתית `.claude/`:** `agents/lesson-qa.md` הונחה לתקן קבצים ישירות אך לא קיבלה הרשאת Edit. המשתמש הכריע: **להוסיף Edit** (לא להגביל את ההנחיה). בוצע.
  - **תיקוני "רצוי/חשוב" נוספים שבוצעו (מכוסים ב"אישור לתקן את כל ההצעות הקונקרטיות"):** `teacher-06.html` – הערת נפילה-חזרה לחיישן BH1750 שחסר ברשימת המצאי הסטנדרטי; `teacher-07.html` – תגית הרכב-עבודה חסרה לכותרת הפרויקט; מספר גיוס Beewise מיושן ("$80M", היה רק סבב 2022) עודכן ל"$170M+ מצטבר" ב-3 מקומות (lesson-07 פעמיים, lesson-13); `commands/expert-panel.md` – נוסף מסלול שלישי מפורש ל"כל הפרויקט" (ממצא של דנה: זו כנראה הסיבה שנועה נכשלה 5 פעמים בסבב הקודם – עומס גדול מדי, לא מתוכנן); `skills/verify-startup/SKILL.md` – האצלת ה-web_search ל-subagent `content-researcher` במקום ביצוע ישיר; "מכניקה בעלת שם" נוספה ל-5 מערכי מורה שחסרו (03/05/07/08/11, אומת שעכשיו כל 17 כוללים).
  - **הרפקטור הטכני של מנוע הגיימיפיקציה (הפריט הגדול ביותר בסבב הזה) – עקרון מפתח: תוספות בלבד, אפס מחיקות.** הבעיה השורשית: `assets/core.js` מגדיר `addLv`/`ansQ` גלובליים, אך כל קובץ שיעור (02-17) מגדיר גרסאות מקומיות משלו בתוך תג `<script>` נטען-מאוחר-יותר שדורסות אותן (function declaration מאוחרת דורסת מוקדמת באותו סקופ) – זו הייתה כבר עובדה ידועה מתועדת (שלב 9ד לגבי `addLv`, "בקשות המשך אחרי 9ו" לגבי `goTo`), אך לא תוקנה. **הפתרון שיושם ב-16 הקבצים (02-17, לא 01):** בלי לגעת בגרסאות המקומיות הקיימות (מסוכן לרסטרקטר 16 קבצים שונים), 3 תוספות מכניות בכל קובץ: (1) בתוך `addLv(n)` הקיים, לפני ה-`}` הסוגר: `GC._lid=N;GC._lv=lv;GC.playSound('leaf');GC._pulsePlantTwin();GC._checkAchievements();`. (2) מיד אחרי `ansQ(...)` הקיים: עטיפה (`const _localAnsQ=ansQ; ansQ=function(...){...}`) שסופרת קומבו ומעניקה בונוס לפי הפרש `lv` לפני/אחרי (לא מספר קבוע – כי כמות העלים לתשובה נכונה משתנה בין קבצים, 5 ברוב, 10 בחלק מ-05-07). (3) בתחתית קוד האתחול (bootstrap), פעם אחת בלבד: `GC._initPlantTwin();GC._initSoundBtn();GC._initA11y()`.
  - **תהליך הביצוע (4 סוכנים מקבילים לרפקטור, ואז 4 סוכני QA מקבילים, ואז 3 סוכני תיקון-עוקב מקבילים):** QA מצא ותיקן בעצמו כמה בעיות אמיתיות: (א) **סיסטמית, קריטית** – `GC._checkStreak()` כותב בונוס +10 ישירות ל-`localStorage['lsN']`, אך המשתנה המקומי `lv` בכל קובץ לא מתעדכן, כך שהקריאה הבאה ל-`addLv` דורסת את הבונוס בשקט. נמצא ע"י QA על 02-05, ואז הופץ תיקון (resync `lv` מ-localStorage אחרי `_checkAchievements()`) לכל 17 הקבצים בסבב תיקון-עוקב ייעודי. (ב) `lesson-09.html` – סוכן הרפקטור המקורי (06-09) פספס אותו לגמרי (0 קריאות GC כלל) בגלל race עם ה-QA שרץ קודם – QA תיקן בעצמו. (ג) `lesson-13.html` – `GC.completeSideQuest('sq-13-pov')` נכשל בשקט (מעולם לא נרשם side quest כי הקובץ לא קורא ל-`GC.initLesson`) – תוקן עם `GC._autoRegisterSideQuest(13)` + resync ייעודי. (ד) `lesson-10/12.html` – `init()` לא שחזר דגלי-השלמה מ-`passport1` בטעינה חוזרת, מה שאפשר הענקת עלים כפולה בלחיצה חוזרת – תוקן. (ה) `lesson-07/08.html` – שני בלוקי קוד ארדואינו חסרים `dir="ltr"` (לא קשור לרפקטור, נמצא אגב הבדיקה) – תוקן. (ו) בנק שאלות הבונוס נשכח מ-lesson-05.html בסבב הראשון (הופרד בטעות לסוכן שלא נשלח) – זוהה ותוקן ידנית בסוף.
  - **אימות סופי:** בדיקת syntax (Node `new Function()`) על כל 32 בלוקי ה-`<script>` (16 קבצים × 2) – כולם תקינים. בדיקת קישוריות מלאה על כל הפרויקט (457 קישורים, 87 קבצי HTML) – 0 שבורים אמיתיים (6 false positives ידועים: template literals ב-JS + טקסט-דוגמה בבוטקאמפ, אותו דפוס שתועד כבר ב-9ג/9ו).
  - **סטארטאפים (אורי):** אומת ומאושר – Prospera/Valmont $300M מדויק, 13/13 זוגות lesson/teacher עקביים, רשימה שחורה נקייה.
  - **פערים שנותרו פתוחים במודע (לא נדרשו/לא טופלו בסבב הזה):** רצף "ברוכים הבאים" כפול ב-`identity.js` בכניסה ל-lesson-01 (יובל, ישן – כבר תועד ב-9ה); ephemeral mini-activities (דיבייט בשיעור 10, "תפוס את ההזיה" בשיעור 12) לא שומרות דגל ב-passport1 ועלולות להעניק עלים כפולים ברענון – דפוס קיים בעוד לומדות, לא ספציפי לרפקטור הזה, לא טופל; `docs/teachers-booklet.docx` עדיין לא-עקוב ולא מתועד – לא ברור מקורו.
  - **גיבוי ל-git:** קומיט לעבודת שלב 9ו (שהמתינה) בוצע לפני תחילת שלב 10; קומיט לעבודת שלב 10 עצמה (27 קבצים) יבוצע בסיום.
- **שלב 10 הושלם. זהו השלב האחרון בתוכנית העבודה המקורית (MASTER-WORKFLOW.md) – התהליך "מסמך הארות → ממשק מלא" הושלם במלואו, משלב 1 (הבנת ההארות) ועד שלב 10 (בקרת איכות כוללת).**
